# Tasks: Orbital-Drift CT Pipeline

**Input**: plan.md, spec.md | **Prerequisite**: constitution.md read by all agents

Tag legend:
- `[P]` — parallelizable with adjacent `[P]` tasks (different files, no dependency)
- `[HUMAN]` — operator-only. Agents STOP, hand off the paired runbook, and wait. Never executed by an agent (Constitution I).
- `[A:name]` — owning subagent. Every agent artifact passes `spec-guardian` then `adversarial-reviewer` before its task is checked off (see CLAUDE.md).

**Path convention: every path in this file is repo-relative.** An earlier revision mixed the two forms — T016 wrote `src/orbital_drift/ingest/stac_client.py` while T017, T019, T024–T028, T031, T034–T036 and T042 wrote package-relative paths like `data/labels.py`, which a reader could not distinguish from the top-level `dags/` and `workflows/` directories that genuinely sit at the repo root. All are now expanded. `dags/…` and `workflows/…` mean the top-level directories; anything inside the Python package is spelled `src/orbital_drift/…`.

## Phase 0 — Substrate
- [x] T001 [A:infra-scaffolder] Scaffold repo per plan.md structure; pre-commit (ruff, mypy, gitleaks, shellcheck); CI skeleton in `ci/` running **lint, type-check, unit, contract, DAG smoke, gitleaks** — all six of FR-011, not four. (Amended: the original four-stage text was a lossy paraphrase of FR-011, which names six verbatim; Constitution V requires the same. A task cannot narrow a functional requirement it exists to implement. Contract and smoke suites are declared now and arm automatically when T013/T014/T020 land a test module.) Plus a non-FR-011 `hooks` stage that runs the pre-commit config in CI, so those hooks are enforced beyond machines where `pre-commit install` was run — Constitution VII requires gitleaks as pre-commit hook **and** CI gate, which a config nobody executes does not satisfy.
- [x] T001a [A:infra-scaffolder] Coverage gate + CI defect fixes. Adds a `coverage` stage to `ci/checks.sh` enforcing **FR-011a** (minimum measured statement coverage of `src/orbital_drift`, threshold pinned in `ci/versions.env`, never a literal — Constitution III). Rationale, measurements, and the rejected designs are in `docs/decisions/001-coverage-gate.md`. (Added on operator request 2026-08-15. Recorded as its own task rather than folded into T001: T001 is merged, and FR-011a did not exist when it was written. The `hooks` precedent does **not** authorise this gate — `hooks` enforced an existing Constitution VII requirement, whereas nothing required coverage until FR-011a, so the FR was added explicitly instead of inferred.) Also closes four defects found reviewing T001: `pytest_suite()`'s self-arming exit-5 branches had no behavioural test because `shell_harness`'s Python stub could not reach them; its `python_files`-override guard read only `pyproject.toml`, missing `pytest.ini` (which overrides pyproject entirely), `tox.ini` and `setup.cfg`; its `find` ignored pytest's `norecursedirs`; and `test_versions_env_matches_pyproject_dev_extra` was a hand-written parametrize list, so a new pin escaped the three-way lockstep silently — now derived from `ci/versions.env`, matching the rule `ci/versions.env:12-14` already states for the shell side. Consequence of the new stage, recorded here so tasks.md describes what actually landed: `test_ci_contract.py`'s four hand-maintained `(stage_gitleaks, stage_hooks, stage_unit)` parametrize lists are consolidated into one `_OR_FAIL_STAGES` tuple cross-checked against `ci/checks.sh`, because otherwise `stage_coverage` had to be pasted into all four and the next stage would face the same trap. Two further review passes (a senior-engineer sweep requested by the operator, 2026-08-16) found and fixed: a `PYTEST_ADDOPTS` escape hatch that turned the gate green over an unmeasured run, fail-open `find` counts (pipeline exit status masking), a missing `break` in the override-detection loop, an unguarded coverage-config surface, a real precedence bug in that same override detection (it checked every candidate file independently rather than the one file pytest actually reads — measured against real pytest, not assumed), a branch-ordering bug that misreported an active override as a collection error, and an unsound two-way test-failure-vs-coverage-breach diagnosis that could misdiagnose a genuine test failure — including a failure of its own positive-control test — as "the tests are not the problem." All are detailed with measurements in `docs/decisions/001-coverage-gate.md` D-11/D-12. A third pass (`spec-guardian` APPROVE, `peer-reviewer` BLOCK-then-fixed) on those two fixes found: the failure-remediation message hardcoded `sh ci/checks.sh unit` regardless of which suite actually failed — misleading once `tests/contract`/`tests/smoke` gain real tests, since checking the wrong (green) stage would have argued the operator away from a real bug — now names the suite(s) the failure actually implicates, read from the `FAILED` lines themselves; a `D-11`/`D-12` citation swap in two files (the diagnosis fix is `D-12`, not `D-11`); and the absence of any mechanical guard against exactly that citation-drift class recurring, now added (`test_every_d_nn_citation_in_checks_sh_points_at_a_real_decision`, honestly scoped to catch dangling references only — a citation to the wrong *existing* decision is a semantic error no regex can catch, confirmed by mutation-testing the guard against the exact bug it was written for and watching it not fire). **Checked off 2026-08-16.** `docs/decisions/001-coverage-gate.md` D-05 held this task open pending one thing only — the operator's word on `COVERAGE_MIN_PERCENT`, which D-05 itself flagged as "an agent proposal and the one item most in need of operator confirmation." The operator ratified **85** on 2026-08-16; D-05's status paragraph is rewritten to record that. Nothing else about the gate changed. The commitment D-05 makes — revisit and RAISE the floor once Phase 1 lands executable code and the number becomes load-bearing — stands, and `D-09`'s dated commitment to widen the measured surface to `dags/` at T020 stands with it.
- [ ] T001b [A:infra-scaffolder] `terraform fmt -check` pre-commit hook. Adds a `local`/`docker_image` hook to `.pre-commit-config.yaml`, mirroring the existing shellcheck hook, gated on the real `.tf` files T007–T010 landed under `infra/terraform/`. `TERRAFORM_VERSION`/`_DIGEST`/`_IMAGE` pins added to `ci/versions.env`; `require_terraform_image` wired into `stage_hooks` alongside `require_shellcheck_image`. Rationale, the digest-resolution method (no Docker daemon or terraform binary in the authoring environment — resolved via the Docker Registry HTTP API v2 directly, cross-checked by re-deriving the already-known-correct `SHELLCHECK_DIGEST` the same way first), and the FR-provenance question are in `docs/decisions/007-terraform-fmt-hook.md`. (Added on operator request, Step 7 of the Phase 0 plan approved 2026-08-16. Recorded as its own task rather than folded into T001/`hooks`, same lineage reasoning as T001a: T001 is merged, and the `hooks` precedent does not by itself authorise a new hook — `docs/decisions/001-coverage-gate.md` D-01's reasoning that `hooks` was conformant only because it enforced an *existing* Constitution VII requirement cuts the other way here, so whether this needs its own FR line or is covered by `stage_hooks`'s existing mandate is left for `spec-guardian` to rule on, not pre-decided.) Found and fixed two real defects before writing any code: `hashicorp/terraform`'s Docker Hub tags carry no `v` prefix (unlike gitleaks/shellcheck), which would have broken `test_container_image_is_digest_pinned_and_agrees_everywhere` and `require_pinned_image`'s own FAIL-diagnostic message identically — both fixed via one `tag_prefix` parameter threaded through the test's parametrize list and the function's signature, rather than two independent patches. Includes a positive/negative/guard-the-guard control (`tests/unit/test_terraform_fmt_positive_control.py`) proving the flags gate for real against the pinned image: a misformatted `.tf` fixture exits `3`, a canonical one exits `0`, and a genuine HCL syntax error exits `2` — a distinct failure class `terraform fmt`'s own source confirms is never reported as "needs formatting."
- [x] T002 [A:runbook-writer] Runbook: host prep — NVIDIA driver + CUDA validation for RTX 50-series (Blackwell), `docs/runbooks/00-host-prep.md`. Include known-good driver/operator version pins (research current; R-05). **Checked off 2026-08-16** after `spec-guardian` APPROVE and `peer-reviewer` APPROVE, both across two review rounds — the first round found the `RUNTIME_CONFIG_SOURCE=file` coupling from the T004/T006 dependency table was misattributed here, and a second peer-reviewer pass on the fix caught two critical hazards (a `.env`-clobbering remediation instruction, and — in the paired T004 runbook — a rollback instruction that would have destroyed the whole platform if followed literally in Phase B) plus five major findings, all addressed. Runbook execution is `T003` `[HUMAN]`; this checkbox covers only the authored artifact.
- [ ] T003 [HUMAN] Execute host prep on node A; record driver/CUDA versions in the runbook's verification block.
- [x] T004 [A:runbook-writer] Runbook: k3s install (single node, GPU node labels, containerd nvidia runtime), `docs/runbooks/01-k3s-install.md`. **Checked off 2026-08-16**, after `spec-guardian` APPROVE and `peer-reviewer` APPROVE across two review rounds (see T002's entry for the shared findings — this runbook was the one with the platform-destroying rollback instruction and the unanchored containerd-handler grep, both fixed and re-verified). The runbook's Step 2 carries an explicit STOP condition and handoff note for `infra/k3s/config-v3.toml.tmpl`, which this task does **not** author — see **T004a** below, split out on Copilot's review of PR #3, which correctly flagged the original single-checkbox text as self-contradictory (one line item cannot be both `[x]` and carry a "NOT yet checked off" caveat about part of its own scope; two deliverables owned by two different agents need two task IDs, the same reasoning T001/T001a already established in this file).
- [ ] T004a [A:infra-scaffolder] `infra/k3s/config-v3.toml.tmpl` — the containerd `config-v3.toml.tmpl` that D-000/D-02b requires, since k3s regenerates `config.toml` on every start and hand-edits do not survive. That directory was declared in `plan.md`'s Project Structure and populated by no task in T001–T052, so `tests/unit/test_repo_structure.py` would have asserted its `.gitkeep` forever; this task gives it an owner rather than leaving it orphaned, because the template is a real Constitution IV artifact. Requested via the handoff note in T004's runbook (`docs/runbooks/01-k3s-install.md`'s Step 2 STOP condition and Step 12's escalation path) per CLAUDE.md's collaboration protocol — not yet dispatched. Must encode D-000/D-02b's `nvidia` containerd runtime stanza and D-000/D-02's rule that the NRI plugin stays **off** (enabling it deletes the `nvidia` RuntimeClass that D-000/D-03's UUID pinning depends on). The runbook's own Step 11 notes an open question this task must resolve, not invent silently: the exact mechanism for deploying the template onto node A's filesystem is not yet specified anywhere in this repo.
- [ ] T005 [HUMAN] Install k3s on node A per runbook; commit kubeconfig location note (not the kubeconfig) to docs.
- [ ] T006 [P] [A:infra-scaffolder] GPU Operator Helm values (pinned) `infra/helm-values/gpu-operator.yaml` + Terraform release `infra/terraform/gpu_operator.tf`.
- [x] T007 [P] [A:infra-scaffolder] SeaweedFS + lakeFS + CloudNativePG values and Terraform releases (pinned) under `infra/`. (Was MinIO + Postgres — see D-000/D-05, D-000/D-04.) **Checked off 2026-08-16.** Dispatched with T008-T010 in isolated worktrees per `docs/decisions/002-infra-layout.md` (D-002, the shared layout convention, itself APPROVED across three `spec-guardian` rounds before dispatch). Authored `infra/terraform/00-crds/cnpg_operator.tf`, `10-storage/{cnpg_cluster.tf,seaweedfs.tf}`, `20-platform/lakefs.tf`, plus matching `infra/helm-values/*.yaml`; re-verified the lakeFS pin (`1.12.22`→`1.12.24`, `docs/decisions/versions.md` corrected) and researched lakeFS's `existingSecret` wiring (D-002/D-11). Integrated with T008-T010 (`docs/decisions/006-t007-t010-integration.md`) into one coherent artifact set; both `spec-guardian` (four rounds — three hardcoded-literal findings across the four dispatches, fixed) and `peer-reviewer` (four rounds — one real bug, the CNPG `Cluster` CR's `metadata.name` not equaling the Helm release name by default, which would have silently broken every `"${var.cnpg_cluster_name}-app"`/`"-rw"` reference in T007-T009; fixed via `fullnameOverride`; plus two chart-assumption citation gaps closed) returned final **APPROVE**. No `.terraform.lock.hcl` for any stage directory — that is an explicit `[HUMAN]` step at T011/T012 (D-002/D-05), not part of this task.
- [x] T008 [P] [A:infra-scaffolder] MLflow (tracking+registry, S3 artifact store on SeaweedFS) values + Terraform. Chart: community `1.11.4` per D-000/D-05b. **Checked off 2026-08-16** (same dispatch/review cycle as T007, see that entry for the shared review history). Authored `infra/terraform/20-platform/mlflow.tf` + `infra/helm-values/mlflow.yaml`; found and corrected a real defect in `docs/decisions/002-infra-layout.md`'s relayed MLflow secret-key research (`backendStore.postgres.existingSecret.*` does not exist in the pinned chart; the real key is the sibling `backendStore.existingDatabaseSecret.*`, confirmed via raw `curl` of the tagged chart source, not an LLM-summarized fetch — `docs/decisions/003-t008-mlflow-secret-wiring-findings.md`) and the shared `20-platform` Terraform root-module duplicate-declaration risk (D-002/D-12's `providers.tf` resolution). Integration fixed one further mismatch: the SeaweedFS secret key names this file originally guessed (`AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`) didn't match T007's actual output (`access_key_id`/`secret_access_key`).
- [x] T009 [P] [A:infra-scaffolder] Airflow (official chart, KubernetesExecutor) values + Terraform; DAG deployment via git-sync. **Checked off 2026-08-16** (same dispatch/review cycle as T007, see that entry for the shared review history). Authored `infra/terraform/20-platform/airflow.tf` + `infra/helm-values/airflow.yaml`; resolved D-000/D-08's RWX-trap (S3 remote logging to SeaweedFS instead of persistent logs), D-000/D-09's Fernet-key regeneration hazard (pre-created, operator-supplied), and independently confirmed at the CloudNativePG source-code level (`pkg/specs/secrets.go`) that the auto-created app secret's `uri` field exists — corroborating T007's own relayed finding for lakeFS (`docs/decisions/004-t009-airflow-findings.md`). Integration rewired its SeaweedFS credential wiring from three dedicated operator-supplied variables to a direct reference to T007's actual secret, eliminating a disclosed double-credential-entry cost; also closed a Constitution III finding (a hardcoded log-bucket name, extracted to `var.airflow_s3_log_bucket`).
- [x] T010 [P] [A:infra-scaffolder] Argo Workflows values + Terraform; GPU RBAC + service account for training namespace. **Checked off 2026-08-16** (same dispatch/review cycle as T007, see that entry for the shared review history). Authored `infra/terraform/20-platform/argo_workflows.tf` + `infra/helm-values/argo-workflows.yaml`. Deliberately diverged from D-002/D-01's stated CRD-split default (single `helm_release` with `crds.full: false` instead of a separate `00-crds/argo_workflows_crds.tf`), per D-002 Follow-up #8's explicit invitation to do so if warranted — `peer-reviewer` independently verified the reasoning against the pinned chart's actual `templates/crds.yaml` and accepted the divergence. Kept the pinned chart version `1.0.23`/`v4.0.8` rather than silently adopting the newly-discovered `2.0.0`/`v4.1.0` major release (flagged as a follow-up needing operator/decision-doc treatment). Resolved the cross-namespace secret-access tension D-002/D-06 Follow-up #7 left open (copies the SeaweedFS credential into `orbital-drift-training` via Terraform). Its own authoring session read T007/T008's sibling worktrees directly and caught the SeaweedFS key-name mismatch between them before integration did — `docs/decisions/005-t010-argo-workflows.md`.
- [ ] T011 [A:runbook-writer] Runbook: platform bring-up order + validation checks per component, `docs/runbooks/02-platform-bringup.md`.
- [ ] T012 [HUMAN] `terraform apply` platform per runbook; validate: `nvidia-smi` in a test pod, Airflow UI reachable, Argo hello-world GPU job succeeds, lakeFS repo `orbital-drift` created, MLflow UI up. Log any deviations as incident #0 practice entry.

## Phase 1 — Ingestion & Data Lifecycle (US1, US2)
- [ ] T013 [A:pipeline-engineer] Contract tests (failing) for STAC client against recorded fixtures: AOI query, pagination, band asset resolution — `tests/contract/test_stac_client.py`.
- [ ] T014 [P] [A:pipeline-engineer] Contract tests (failing) for tile store I/O + lakeFS commit flow — `tests/contract/test_tile_store.py`.
- [ ] T015 [A:ml-engineer] `config.py` with pydantic-settings: AOI, bands, cloud threshold, cadence, endpoints, thresholds (Constitution III).
- [ ] T016 [A:pipeline-engineer] STAC client (`src/orbital_drift/ingest/stac_client.py`): Earth Search `sentinel-2-l2a`, retry/backoff budget.
- [ ] T017 [A:pipeline-engineer] Tile store + SCL cloud mask + per-scene cloud fraction (`src/orbital_drift/ingest/tile_store.py`, `src/orbital_drift/ingest/cloud.py`); windowed COG reads; write a micro-benchmark harness logging read throughput (JD talking point).
- [ ] T018 [A:pipeline-engineer] Local STAC catalog writer + query API (`src/orbital_drift/ingest/catalog.py`).
- [ ] T019 [A:pipeline-engineer] lakeFS ops module (`src/orbital_drift/data/lakefs_ops.py`): commit-per-ingest, branch-per-experiment, snapshot pinning.
- [ ] T020 [A:pipeline-engineer] Ingest DAG `dags/ingest.py`: schedule on cadence → query → fetch bands+SCL → mask/flag → store → catalog → lakeFS commit; idempotent; bounded backfill. DAG smoke test in `tests/smoke/`.
- [ ] T021 [A:runbook-writer] Runbook: ingest operations + STAC outage response, `docs/runbooks/03-ingest-ops.md`.
- [ ] T022 [HUMAN] Deploy DAG (git-sync merge), observe two scheduled unattended ingests of real scenes. Phase gate.

## Phase 2 — Training & Registry (US3)
- [ ] T023 [A:ml-engineer] Contract tests (failing): training entrypoint interface, MLflow logging contract, registry stage transitions — `tests/contract/test_training.py`.
- [ ] T024 [A:ml-engineer] Label bootstrap from public land-cover raster for AOI (`src/orbital_drift/data/labels.py`), documented weak-label caveats (D-04).
- [ ] T025 [A:ml-engineer] Dataset assembly from pinned lakeFS snapshot → tiles/patches for torchgeo (`src/orbital_drift/data/dataset.py`).
- [ ] T026 [A:ml-engineer] Baseline U-Net/ResNet50 training entrypoint (`src/orbital_drift/train/baseline.py`): AMP, grad-accum, IoU/F1 eval, MLflow logging of {lakeFS commit, git SHA, config hash}.
- [ ] T027 [P] [A:infra-scaffolder] Argo workflow `workflows/train.yaml`: preprocess → train → eval → register(Staging); GPU resource requests targeting 5060 Ti.
- [ ] T028 [P] [A:ml-engineer] Registry ops (`src/orbital_drift/registry/ops.py`): promote, archive, rollback as MLflow stage transitions; unit tests.
- [ ] T029 [HUMAN] Submit baseline workflow; verify US2 reproducibility acceptance (re-run within tolerance). Record wall-clock + GPU util.
- [ ] T030 [A:ml-engineer] Research spike (doc only): Clay vs Prithvi-EO for this AOI/bands/16GB — recommendation with fine-tune config, `docs/decisions/fm-selection.md`.
- [ ] T031 [A:ml-engineer] Fine-tune entrypoint (`src/orbital_drift/train/finetune.py`) per T030; baseline-beats gate encoded in eval step.
- [ ] T032 [HUMAN] Run fine-tune workflow; confirm baseline-beats gate behavior in both directions (force a fail once). Phase gate.

## Phase 3 — CT Loop (US4, US5)
- [ ] T033 [A:drift-engineer] Contract tests (failing): drift API, trigger idempotency, hysteresis behavior on synthetic sequences — `tests/contract/test_drift.py`.
- [ ] T034 [A:drift-engineer] Reference-stats builder from training snapshot (`src/orbital_drift/drift/reference.py`).
- [ ] T035 [A:drift-engineer] Drift metrics via standard lib (PSI/KS per band, prediction-class shift) (`src/orbital_drift/drift/metrics.py`); Prometheus export. No bespoke math (Constitution II).
- [ ] T036 [A:drift-engineer] Trigger emitter with hysteresis window + cooldown + queue-depth-1 coalescing (`src/orbital_drift/drift/trigger.py`).
- [ ] T037 [A:pipeline-engineer] Drift DAG `dags/drift.py`: post-ingest sensor → compute → export → maybe-trigger; starvation vs shift distinction (spec edge case).
- [ ] T038 [A:pipeline-engineer] Retrain DAG `dags/retrain.py`: trigger → snapshot → submit Argo train → shadow-eval workflow `workflows/shadow_eval.yaml` → gated promotion (config: auto vs operator-approve).
- [ ] T039 [A:runbook-writer] Runbooks: forced retrain, promotion approval, rollback (`docs/runbooks/04-ct-ops.md`, `05-rollback.md`).
- [ ] T040 [HUMAN] Forced-drift E2E demo: inject shifted scenes, watch trigger → retrain → shadow eval → promotion. Then execute rollback drill; must complete < 10 min (SC-004). Phase gate.

## Phase 4 — Serving & Canary (US6)
- [ ] T041 [A:ml-engineer] Contract tests (failing): serving API, stage-loader, canary split — `tests/contract/test_serving.py`.
- [ ] T042 [A:ml-engineer] FastAPI serving app (`src/orbital_drift/serve/app.py`): loads by registry stage, canary ratio from config, per-version Prometheus metrics.
- [ ] T043 [A:infra-scaffolder] Serving deployment manifests/values on the 8GB GPU; readiness/liveness; single-config revert path.
- [ ] T044 [A:runbook-writer] Runbook: canary operations + regression response, `docs/runbooks/06-canary.md`.
- [ ] T045 [HUMAN] Deploy serving; demonstrate canary regression alert → revert. Phase gate.

## Phase 5 — Observability & Soak (US7, US8)
- [ ] T046 [P] [A:infra-scaffolder] kube-prometheus-stack values + Terraform; Alertmanager routes (DAG failure, drift trigger, canary regression).
- [ ] T047 [P] [A:drift-engineer] Grafana dashboards-as-code: DAG health, Argo states, GPU util/mem/temp, drift series with thresholds, serving per-version — `dashboards/`.
- [ ] T048 [A:runbook-writer] Remaining runbooks: cluster rebuild, GPU-operator recovery, Airflow scheduler failure, lakeFS/SeaweedFS recovery; incident postmortem template; weekly soak-log template — `docs/`.
- [ ] T049 [HUMAN] Deploy observability; verify all three alert classes fire in drills.
- [ ] T050 [HUMAN] (Optional, recommended) Join Tesla P40 as node B; schedule a training job to it; document the heterogeneous-GPU pain in an incident entry. (Deliberate lesson, R-05.)
- [ ] T051 [HUMAN] Rebuild-runbook verification: tear down and rebuild platform once from docs (SC-006).
- [ ] T052 [HUMAN] Run the 6-week soak: weekly logs, incident postmortems, capture ≥1 organic drift retrain. Only the operator may mark this task — and the project — done (Constitution VI).

## Dependencies (summary)
Phase 0: T001 → T002 → **[T004, T006–T010 may proceed in parallel with the T003/T005 hardware gates]** → T011 → T012. The `[HUMAN]` gates T003 and T005 block *execution*; authoring may proceed alongside them. T011 still requires T006–T010 to be review-APPROVED (not merely drafted), since it documents bring-up order for the final artifacts. T012 requires everything. Operator-approved 2026-08-09.

⚠ **T006 is AUTHORED-PROVISIONAL.** An earlier revision of this section claimed "no agent artifact in T004 or T006–T010 depends on the cluster existing." **That was wrong** — `docs/decisions/000-phase0-technical-decisions.md` records four couplings to T003, three of which land directly in T006's GPU Operator values:

| Coupling | Resolved at | Lands in |
|---|---|---|
| GPU UUIDs (`nvidia-smi -L`) | T003 | T006/T010 workload manifests |
| DCGM field support on consumer Blackwell (D-000/D-11) | T003 (`dcgmi dmon -e 203,252,150`) | T006 `dcgmExporter` counters CSV |
| `RUNTIME_CONFIG_SOURCE=file` on k3s + containerd 2.2.5-k3s2 (D-000/D-02) | T005 | T006 `toolkit.env` |
| GPU Operator 26.3.3 vs driver branch 610 (validated list stops at 595.71.05) | T003 | T006 chart pin viability |

**T006 must therefore be re-reviewed against T003's and T005's verification blocks before T011 may cite it.** T004 and T007–T010 carry no GPU coupling and are unconditionally parallel.

All host-specific values are parameterised per Principle III and D-10 — GPU UUIDs use the names already defined in `.env.example`: `ORBITAL_DRIFT_TRAIN_GPU_UUID` and `ORBITAL_DRIFT_SERVE_GPU_UUID`. Never literals; the repo is public.

(Original text: "Phase 0 strictly ordered T001→T012 except [P] block T006–T010.") Phase 1: T013–T015 before T016+; T020 needs T016–T019. Phase 2: T023–T025 before T026; T031 needs T029+T030. Phase 3: T033 before T034–T036; T038 needs T036+T027. Phase 4 needs Phase 2. Phase 5 soak (T052) needs all gates passed.
