# Tasks: Orbital-Drift CT Pipeline

**Input**: plan.md, spec.md | **Prerequisite**: constitution.md read by all agents

Tag legend:
- `[P]` — parallelizable with adjacent `[P]` tasks (different files, no dependency)
- `[HUMAN]` — operator-only. Agents STOP, hand off the paired runbook, and wait. Never executed by an agent (Constitution I).
- `[A:name]` — owning subagent. Every agent artifact passes `spec-guardian` then `adversarial-reviewer` before its task is checked off (see CLAUDE.md).

**Path convention: every path in this file is repo-relative.** An earlier revision mixed the two forms — T016 wrote `src/orbital_drift/ingest/stac_client.py` while T017, T019, T024–T028, T031, T034–T036 and T042 wrote package-relative paths like `data/labels.py`, which a reader could not distinguish from the top-level `dags/` and `workflows/` directories that genuinely sit at the repo root. All are now expanded. `dags/…` and `workflows/…` mean the top-level directories; anything inside the Python package is spelled `src/orbital_drift/…`.

## Phase 0 — Substrate
- [x] T001 [A:infra-scaffolder] Scaffold repo per plan.md structure; pre-commit (ruff, mypy, gitleaks, shellcheck); CI skeleton in `ci/` running **lint, type-check, unit, contract, DAG smoke, gitleaks** — all six of FR-011, not four. (Amended: the original four-stage text was a lossy paraphrase of FR-011, which names six verbatim; Constitution V requires the same. A task cannot narrow a functional requirement it exists to implement. Contract and smoke suites are declared now and arm automatically when T013/T014/T020 land a test module.) Plus a non-FR-011 `hooks` stage that runs the pre-commit config in CI, so those hooks are enforced beyond machines where `pre-commit install` was run — Constitution VII requires gitleaks as pre-commit hook **and** CI gate, which a config nobody executes does not satisfy.
- [x] T001a [A:infra-scaffolder] Coverage gate + CI defect fixes. Adds a `coverage` stage to `ci/checks.sh` enforcing **FR-011a** (minimum measured statement and branch coverage of `src/orbital_drift`, as one combined rate, threshold pinned in `ci/versions.env`, never a literal — Constitution III; branch measurement added under RB-008 part 3 on 2026-08-22, with no threshold VALUE change — see `docs/decisions/001-coverage-gate.md` D-14 for the one-combined-bar decision). Rationale, measurements, and the rejected designs are in `docs/decisions/001-coverage-gate.md`. (Added on operator request 2026-08-15. Recorded as its own task rather than folded into T001: T001 is merged, and FR-011a did not exist when it was written. The `hooks` precedent does **not** authorise this gate — `hooks` enforced an existing Constitution VII requirement, whereas nothing required coverage until FR-011a, so the FR was added explicitly instead of inferred.) Also closes four defects found reviewing T001: `pytest_suite()`'s self-arming exit-5 branches had no behavioural test because `shell_harness`'s Python stub could not reach them; its `python_files`-override guard read only `pyproject.toml`, missing `pytest.ini` (which overrides pyproject entirely), `tox.ini` and `setup.cfg`; its `find` ignored pytest's `norecursedirs`; and `test_versions_env_matches_pyproject_dev_extra` was a hand-written parametrize list, so a new pin escaped the three-way lockstep silently — now derived from `ci/versions.env`, matching the rule `ci/versions.env:12-14` already states for the shell side. Consequence of the new stage, recorded here so tasks.md describes what actually landed: `test_ci_contract.py`'s four hand-maintained `(stage_gitleaks, stage_hooks, stage_unit)` parametrize lists are consolidated into one `_OR_FAIL_STAGES` tuple cross-checked against `ci/checks.sh`, because otherwise `stage_coverage` had to be pasted into all four and the next stage would face the same trap. Two further review passes (a senior-engineer sweep requested by the operator, 2026-08-16) found and fixed: a `PYTEST_ADDOPTS` escape hatch that turned the gate green over an unmeasured run, fail-open `find` counts (pipeline exit status masking), a missing `break` in the override-detection loop, an unguarded coverage-config surface, a real precedence bug in that same override detection (it checked every candidate file independently rather than the one file pytest actually reads — measured against real pytest, not assumed), a branch-ordering bug that misreported an active override as a collection error, and an unsound two-way test-failure-vs-coverage-breach diagnosis that could misdiagnose a genuine test failure — including a failure of its own positive-control test — as "the tests are not the problem." All are detailed with measurements in `docs/decisions/001-coverage-gate.md` D-11/D-12. A third pass (`spec-guardian` APPROVE, `peer-reviewer` BLOCK-then-fixed) on those two fixes found: the failure-remediation message hardcoded `sh ci/checks.sh unit` regardless of which suite actually failed — misleading once `tests/contract`/`tests/smoke` gain real tests, since checking the wrong (green) stage would have argued the operator away from a real bug — now names the suite(s) the failure actually implicates, read from the `FAILED` lines themselves; a `D-11`/`D-12` citation swap in two files (the diagnosis fix is `D-12`, not `D-11`); and the absence of any mechanical guard against exactly that citation-drift class recurring, now added (`test_every_d_nn_citation_in_checks_sh_points_at_a_real_decision`, honestly scoped to catch dangling references only — a citation to the wrong *existing* decision is a semantic error no regex can catch, confirmed by mutation-testing the guard against the exact bug it was written for and watching it not fire). **Checked off 2026-08-16.** `docs/decisions/001-coverage-gate.md` D-05 held this task open pending one thing only — the operator's word on `COVERAGE_MIN_PERCENT`, which D-05 itself flagged as "an agent proposal and the one item most in need of operator confirmation." The operator ratified **85** on 2026-08-16; D-05's status paragraph is rewritten to record that. Nothing else about the gate changed. The commitment D-05 makes — revisit and RAISE the floor once Phase 1 lands executable code and the number becomes load-bearing — stands, and `D-09`'s dated commitment to widen the measured surface to `dags/` at T020 stands with it.
- [x] T001b [A:infra-scaffolder] `terraform fmt -check` pre-commit hook. Adds a `local`/`docker_image` hook to `.pre-commit-config.yaml`, mirroring the existing shellcheck hook, gated on the real `.tf` files T007–T010 landed under `infra/terraform/`. `TERRAFORM_VERSION`/`_DIGEST`/`_IMAGE` pins added to `ci/versions.env`; `require_terraform_image` wired into `stage_hooks` alongside `require_shellcheck_image`. Rationale, the digest-resolution method (no Docker daemon or terraform binary in the authoring environment — resolved via the Docker Registry HTTP API v2 directly, cross-checked by re-deriving the already-known-correct `SHELLCHECK_DIGEST` the same way first), and the FR-provenance question are in `docs/decisions/007-terraform-fmt-hook.md`. (Added on operator request, Step 7 of the Phase 0 plan approved 2026-08-16. Recorded as its own task rather than folded into T001/`hooks`, same lineage reasoning as T001a: T001 is merged, and the `hooks` precedent does not by itself authorise a new hook — `docs/decisions/001-coverage-gate.md` D-01's reasoning that `hooks` was conformant only because it enforced an *existing* Constitution VII requirement cuts the other way here; `spec-guardian` ruled 2026-08-22 (T001b-closure PR): **FR-011b** added per the D-001/D-01 precedent — ruling recorded in `docs/decisions/007-terraform-fmt-hook.md` D-007/06.) Found and fixed two real defects before writing any code: `hashicorp/terraform`'s Docker Hub tags carry no `v` prefix (unlike gitleaks/shellcheck), which would have broken `test_container_image_is_digest_pinned_and_agrees_everywhere` and `require_pinned_image`'s own FAIL-diagnostic message identically — both fixed via one `tag_prefix` parameter threaded through the test's parametrize list and the function's signature, rather than two independent patches. Includes a positive/negative/guard-the-guard control (`tests/unit/test_terraform_fmt_positive_control.py`) proving the flags gate for real against the pinned image: a misformatted `.tf` fixture exits `3`, a canonical one exits `0`, and a genuine HCL syntax error exits `2` — a distinct failure class `terraform fmt`'s own source confirms is never reported as "needs formatting." Checked off 2026-08-22: both reviewers APPROVE (spec-guardian ruling D-007/06 → FR-011b; adversarial-reviewer round 2), and the flip condition met — doc 007 "Not resolved here" item 2 closed by CI run 32574454828 (attempt 1, success), in which the three container positive controls in `tests/unit/test_terraform_fmt_positive_control.py` executed against the pinned image.
- [x] T002 [A:runbook-writer] Runbook: host prep — NVIDIA driver + CUDA validation for RTX 50-series (Blackwell), `docs/runbooks/00-host-prep.md`. Include known-good driver/operator version pins (research current; R-05). **Checked off 2026-08-16** after `spec-guardian` APPROVE and `peer-reviewer` APPROVE, both across two review rounds — the first round found the `RUNTIME_CONFIG_SOURCE=file` coupling from the T004/T006 dependency table was misattributed here, and a second peer-reviewer pass on the fix caught two critical hazards (a `.env`-clobbering remediation instruction, and — in the paired T004 runbook — a rollback instruction that would have destroyed the whole platform if followed literally in Phase B) plus five major findings, all addressed. Runbook execution is `T003` `[HUMAN]`; this checkbox covers only the authored artifact.
- [ ] T003 [HUMAN] Execute host prep on node A; record driver/CUDA versions in the runbook's verification block.
- [x] T004 [A:runbook-writer] Runbook: k3s install (single node, GPU node labels, containerd nvidia runtime), `docs/runbooks/01-k3s-install.md`. **Checked off 2026-08-16**, after `spec-guardian` APPROVE and `peer-reviewer` APPROVE across two review rounds (see T002's entry for the shared findings — this runbook was the one with the platform-destroying rollback instruction and the unanchored containerd-handler grep, both fixed and re-verified). The runbook's Step 2 carries an explicit STOP condition and handoff note for `infra/k3s/config-v3.toml.tmpl`, which this task does **not** author — see **T004a** below, split out on Copilot's review of PR #3, which correctly flagged the original single-checkbox text as self-contradictory (one line item cannot be both `[x]` and carry a "NOT yet checked off" caveat about part of its own scope; two deliverables owned by two different agents need two task IDs, the same reasoning T001/T001a already established in this file).
- [x] T004a [A:infra-scaffolder] `infra/k3s/config-v3.toml.tmpl` — the containerd `config-v3.toml.tmpl` that D-000/D-02b requires, since k3s regenerates `config.toml` on every start and hand-edits do not survive. That directory was declared in `plan.md`'s Project Structure and populated by no task in T001–T052, so `tests/unit/test_repo_structure.py` would have asserted its `.gitkeep` forever; this task gives it an owner rather than leaving it orphaned, because the template is a real Constitution IV artifact. Requested via the handoff note in T004's runbook (`docs/runbooks/01-k3s-install.md`'s Step 2 STOP condition and Step 12's escalation path) per CLAUDE.md's collaboration protocol. **Checked off 2026-08-22** (authorization G-0 + RB-007): `spec-guardian` APPROVE (two Minors fixed — containerd pin provenance added to versions.md, template header defers to doc 008's live Status); `adversarial-reviewer` round 1 BLOCK (the six tests were mutation-transparent to guard removal/inversion, a static-stub base replacement, and options-table removal), fix cycle 1 hardened the suite (all four mutations re-run unmodified now red, each on its targeting assertion), round 2 APPROVE; CI run 32575017786 green. Deployment mechanism is PROPOSED in `docs/decisions/008-config-v3-deployment.md` D-03 — operator ratification is a precondition of T005, not of this checkbox. Must encode D-000/D-02b's `nvidia` containerd runtime stanza and D-000/D-02's rule that the NRI plugin stays **off** (enabling it deletes the `nvidia` RuntimeClass that D-000/D-03's UUID pinning depends on). The runbook's Step 11 open question — the exact mechanism for deploying the template onto node A's filesystem, previously specified nowhere in the repo — is resolved as required: PROPOSED (not silently invented) in `docs/decisions/008-config-v3-deployment.md` D-03, awaiting operator ratification before T005.
- [ ] T005 [HUMAN] Install k3s on node A per runbook; commit kubeconfig location note (not the kubeconfig) to docs.
- [ ] T006 [P] [A:infra-scaffolder] GPU Operator Helm values (pinned) `infra/helm-values/gpu-operator.yaml` + Terraform release `infra/terraform/gpu_operator.tf`.
- [x] T007 [P] [A:infra-scaffolder] SeaweedFS + lakeFS + CloudNativePG values and Terraform releases (pinned) under `infra/`. (Was MinIO + Postgres — see D-000/D-05, D-000/D-04.) **Checked off 2026-08-16.** Dispatched with T008-T010 in isolated worktrees per `docs/decisions/002-infra-layout.md` (D-002, the shared layout convention, itself APPROVED across three `spec-guardian` rounds before dispatch). Authored `infra/terraform/00-crds/cnpg_operator.tf`, `10-storage/{cnpg_cluster.tf,seaweedfs.tf}`, `20-platform/lakefs.tf`, plus matching `infra/helm-values/*.yaml`; re-verified the lakeFS pin (`1.12.22`→`1.12.24`, `docs/decisions/versions.md` corrected) and researched lakeFS's `existingSecret` wiring (D-002/D-11). Integrated with T008-T010 (`docs/decisions/006-t007-t010-integration.md`) into one coherent artifact set; both `spec-guardian` (four rounds — three hardcoded-literal findings across the four dispatches, fixed) and `peer-reviewer` (four rounds — one real bug, the CNPG `Cluster` CR's `metadata.name` not equaling the Helm release name by default, which would have silently broken every `"${var.cnpg_cluster_name}-app"`/`"-rw"` reference in T007-T009; fixed via `fullnameOverride`; plus two chart-assumption citation gaps closed) returned final **APPROVE**. No `.terraform.lock.hcl` for any stage directory — that is an explicit `[HUMAN]` step at T011/T012 (D-002/D-05), not part of this task.
- [x] T008 [P] [A:infra-scaffolder] MLflow (tracking+registry, S3 artifact store on SeaweedFS) values + Terraform. Chart: community `1.11.4` per D-000/D-05b. **Checked off 2026-08-16** (same dispatch/review cycle as T007, see that entry for the shared review history). Authored `infra/terraform/20-platform/mlflow.tf` + `infra/helm-values/mlflow.yaml`; found and corrected a real defect in `docs/decisions/002-infra-layout.md`'s relayed MLflow secret-key research (`backendStore.postgres.existingSecret.*` does not exist in the pinned chart; the real key is the sibling `backendStore.existingDatabaseSecret.*`, confirmed via raw `curl` of the tagged chart source, not an LLM-summarized fetch — `docs/decisions/003-t008-mlflow-secret-wiring-findings.md`) and the shared `20-platform` Terraform root-module duplicate-declaration risk (D-002/D-12's `providers.tf` resolution). Integration fixed one further mismatch: the SeaweedFS secret key names this file originally guessed (`AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`) didn't match T007's actual output (`access_key_id`/`secret_access_key`).
- [x] T009 [P] [A:infra-scaffolder] Airflow (official chart, KubernetesExecutor) values + Terraform; DAG deployment via git-sync. **Checked off 2026-08-16** (same dispatch/review cycle as T007, see that entry for the shared review history). Authored `infra/terraform/20-platform/airflow.tf` + `infra/helm-values/airflow.yaml`; resolved D-000/D-08's RWX-trap (S3 remote logging to SeaweedFS instead of persistent logs), D-000/D-09's Fernet-key regeneration hazard (pre-created, operator-supplied), and independently confirmed at the CloudNativePG source-code level (`pkg/specs/secrets.go`) that the auto-created app secret's `uri` field exists — corroborating T007's own relayed finding for lakeFS (`docs/decisions/004-t009-airflow-findings.md`). Integration rewired its SeaweedFS credential wiring from three dedicated operator-supplied variables to a direct reference to T007's actual secret, eliminating a disclosed double-credential-entry cost; also closed a Constitution III finding (a hardcoded log-bucket name, extracted to `var.airflow_s3_log_bucket`).
- [x] T010 [P] [A:infra-scaffolder] Argo Workflows values + Terraform; GPU RBAC + service account for training namespace. **Checked off 2026-08-16** (same dispatch/review cycle as T007, see that entry for the shared review history). Authored `infra/terraform/20-platform/argo_workflows.tf` + `infra/helm-values/argo-workflows.yaml`. Deliberately diverged from D-002/D-01's stated CRD-split default (single `helm_release` with `crds.full: false` instead of a separate `00-crds/argo_workflows_crds.tf`), per D-002 Follow-up #8's explicit invitation to do so if warranted — `peer-reviewer` independently verified the reasoning against the pinned chart's actual `templates/crds.yaml` and accepted the divergence. Kept the pinned chart version `1.0.23`/`v4.0.8` rather than silently adopting the newly-discovered `2.0.0`/`v4.1.0` major release (flagged as a follow-up needing operator/decision-doc treatment). Resolved the cross-namespace secret-access tension D-002/D-06 Follow-up #7 left open (copies the SeaweedFS credential into `orbital-drift-training` via Terraform). Its own authoring session read T007/T008's sibling worktrees directly and caught the SeaweedFS key-name mismatch between them before integration did — `docs/decisions/005-t010-argo-workflows.md`.
- [ ] T011 [A:runbook-writer] Runbook: platform bring-up order + validation checks per component, `docs/runbooks/02-platform-bringup.md`.
- [ ] T012 [HUMAN] `terraform apply` platform per runbook; validate: `nvidia-smi` in a test pod, Airflow UI reachable, Argo hello-world GPU job succeeds, lakeFS repo `orbital-drift` created, MLflow UI up. Log any deviations as incident #0 practice entry.

## Phase 1 — Ingestion & Data Lifecycle (US1, US2)

Status as of 2026-09-01 (RB-010): this phase's code was authored across PR#16/#17 without following the task-by-task authoring/review sequence below — see RB-010 in docs/decision-log.md. Per-task annotations here are the retroactive, evidence-based status; AUTHORED-PROVISIONAL tasks require spec-guardian + adversarial-reviewer review before any checkbox may flip to [x].

- [ ] T013 [A:pipeline-engineer] Contract tests (failing) for STAC client against recorded fixtures: AOI query, pagination, band asset resolution — `tests/contract/test_stac_client.py`. **RB-010 audit status (2026-09-01): AUTHORED-PROVISIONAL — PARTIAL.** `tests/contract/test_stac_client.py` exists (9 tests, verified via `pytest --collect-only`; AOI-query and band-asset-resolution coverage present) but no pagination test exists, matching T016's own gap. Pending spec-guardian + adversarial-reviewer review (RB-010, `docs/decision-log.md`) before any checkbox flips to `[x]`.
- [ ] T014 [P] [A:pipeline-engineer] Contract tests (failing) for tile store I/O + lakeFS commit flow — `tests/contract/test_tile_store.py`. **RB-010 audit status (2026-09-01): AUTHORED-PROVISIONAL — PARTIAL.** `tests/contract/test_tile_store.py` covers tile store + cloud mask only (3 tests); lakeFS testing lives in the separately-created `tests/contract/test_lakefs_ops.py` instead — functionally covered but structurally split from what this task names. Pending review per RB-010.
- [ ] T015 [A:ml-engineer] `config.py` with pydantic-settings: AOI, bands, cloud threshold, cadence, endpoints, thresholds (Constitution III). **RB-010 audit status (2026-09-01): AUTHORED-PROVISIONAL — DONE.** Real `pydantic_settings.BaseSettings` implementation (`src/orbital_drift/config.py`), tested in `tests/unit/test_config.py`. Task scope met; still pending spec-guardian + adversarial-reviewer review per RB-010 before checkbox flip. **Superseded 2026-09-05 (RB-012):** this line previously read "no other `src/orbital_drift` module imports it yet (confirmed by search)". RB-010 Parts 5a/5b/5c wired eight modules after Part 1 reconciled these docs, and no later part revisited them — measured at HEAD, `ingest/{stac_client,tile_store,cloud}.py`, `data/dataset.py`, `drift/metrics.py`, `train/baseline.py`, `registry/ops.py` and `serve/app.py` all import `orbital_drift.config`. The residual gap is narrower and is now owned by **T061**: five sites recorded in `docs/decisions/012-rb010-part6-config-wiring-followups.md`, plus `data/lakefs_ops.py` and `drift/trigger.py`, which hold FR-012 values and consult no config at all.
- [ ] T016 [A:pipeline-engineer] STAC client (`src/orbital_drift/ingest/stac_client.py`): Earth Search `sentinel-2-l2a`, retry/backoff budget. **RB-010 audit status (2026-09-01): AUTHORED-PROVISIONAL — PARTIAL.** Real retry/backoff (exponential, budgeted) exists, but there is no pagination — a single POST per call, no next-link following. Pending review per RB-010.
- [ ] T017 [A:pipeline-engineer] Tile store + SCL cloud mask + per-scene cloud fraction (`src/orbital_drift/ingest/tile_store.py`, `src/orbital_drift/ingest/cloud.py`); windowed COG reads; write a micro-benchmark harness logging read throughput (JD talking point). **RB-010 audit status (2026-09-01): AUTHORED-PROVISIONAL — PARTIAL.** Cloud mask/fraction real and tested; tile store does local `.npy` save/load only — no rasterio, no COG, no windowed reads, no S3, despite this task naming windowed COG reads (confirmed by search: no `rasterio`/`cog`/`boto3`/`s3://` reference anywhere under `src/orbital_drift/ingest/`). Pending review per RB-010.
- [ ] T018 [A:pipeline-engineer] Local STAC catalog writer + query API (`src/orbital_drift/ingest/catalog.py`). **RB-010 audit status (2026-09-01): NOT STARTED.** `src/orbital_drift/ingest/catalog.py` does not exist. `src/orbital_drift/ports/catalog.py` (added by PR#17) is a different, in-memory-only `Protocol` fake (`InMemorySceneCatalog`) and does NOT fill this gap.
- [ ] T019 [A:pipeline-engineer] lakeFS ops module (`src/orbital_drift/data/lakefs_ops.py`): commit-per-ingest, branch-per-experiment, snapshot pinning. **RB-010 audit status (2026-09-01): AUTHORED-PROVISIONAL — PARTIAL/SIMULATED.** Implements all three named operations with real logic and good tests, but it is a pure local simulation — `commit_scene` fabricates its return value via `hashlib.sha256`, and no `lakefs`/`lakefs-sdk` import exists anywhere in the repo (confirmed by search), nor is either declared as a dependency in `pyproject.toml`. No real lakeFS integration exists. Pending review per RB-010.
- [ ] T020 [A:pipeline-engineer] Ingest DAG `dags/ingest.py`: schedule on cadence → query → fetch bands+SCL → mask/flag → store → catalog → lakeFS commit; idempotent; bounded backfill. DAG smoke test in `tests/smoke/`. **RB-010 audit status (2026-09-01): NOT STARTED.** `dags/` contains only `.gitkeep`.
- [ ] T021 [A:runbook-writer] Runbook: ingest operations + STAC outage response, `docs/runbooks/03-ingest-ops.md`. **RB-010 audit status (2026-09-01): NOT STARTED.** `docs/runbooks/03-ingest-ops.md` does not exist.
- [ ] T022 [HUMAN] Deploy DAG (git-sync merge), observe two scheduled unattended ingests of real scenes. Phase gate. **RB-010 audit status (2026-09-01): NOT STARTED**, blocked on T020 + cluster (T003/T005/T012).

## Phase 2 — Training & Registry (US3)
- [ ] T023 [A:ml-engineer] Contract tests (failing): training entrypoint interface, MLflow logging contract, registry stage transitions — `tests/contract/test_training.py`. **RB-010 audit status (2026-09-01): AUTHORED-PROVISIONAL — PARTIAL.** `tests/contract/test_training.py` (5 tests, verified via `pytest --collect-only`) covers dataset patch generation, model forward pass, training-epoch execution, and registry stage transitions; the "MLflow logging contract" test (`test_provenance_metadata_triple_generation`) only checks the dict shape returned by `train/baseline.py`'s local `build_run_metadata` helper — no real MLflow call exists anywhere (see T026). Pending review per RB-010.
- [ ] T024 [A:ml-engineer] Label bootstrap from public land-cover raster for AOI (`src/orbital_drift/data/labels.py`), documented weak-label caveats (D-04). **RB-010 audit status (2026-09-01): NOT STARTED.** `src/orbital_drift/data/labels.py` does not exist.
- [ ] T025 [A:ml-engineer] Dataset assembly from pinned lakeFS snapshot → tiles/patches for torchgeo (`src/orbital_drift/data/dataset.py`). **RB-010 audit status (2026-09-01): AUTHORED-PROVISIONAL — PARTIAL.** `Sentinel2PatchDataset` is real and tested but does not use torchgeo (not a declared dependency, not imported anywhere in the repo — confirmed by search) and does not pull from a lakeFS snapshot — it takes a raw in-memory `numpy.ndarray` directly. Pending review per RB-010.
- [ ] T026 [A:ml-engineer] Baseline U-Net/ResNet50 training entrypoint (`src/orbital_drift/train/baseline.py`): AMP, grad-accum, IoU/F1 eval, MLflow logging of {lakeFS commit, git SHA, config hash}. **RB-010 audit status (2026-09-01): AUTHORED-PROVISIONAL — PARTIAL.** AMP (`torch.amp.autocast`/`GradScaler`), gradient accumulation, and IoU/F1 (`compute_iou_f1`) are all real and tested; there is no MLflow logging — `mlflow` is not imported anywhere in `src/` and is not a declared dependency (confirmed by search) — only a local metadata dict is built (`build_run_metadata`). Pending review per RB-010.
- [ ] T027 [P] [A:infra-scaffolder] Argo workflow `workflows/train.yaml`: preprocess → train → eval → register(Staging); GPU resource requests targeting 5060 Ti. **RB-010 audit status (2026-09-01): NOT STARTED.** `workflows/` contains only `.gitkeep`.
- [ ] T028 [P] [A:ml-engineer] Registry ops (`src/orbital_drift/registry/ops.py`): promote, archive, rollback as MLflow stage transitions; unit tests. **RB-010 audit status (2026-09-01): AUTHORED-PROVISIONAL — PARTIAL/SIMULATED.** Implements all transitions with good tests, but is a pure in-process dict simulation (`self._mock_registry`) — no `mlflow` import anywhere, not a declared dependency. Pending review per RB-010.
- [ ] T029 [HUMAN] Submit baseline workflow; verify US2 reproducibility acceptance (re-run within tolerance). Record wall-clock + GPU util. **RB-010 audit status (2026-09-01): NOT STARTED**, blocked on T027 (no workflow to submit) and the cluster gates (T003/T005/T012).
- [ ] T030 [A:ml-engineer] Research spike (doc only): Clay vs Prithvi-EO for this AOI/bands/16GB — recommendation with fine-tune config, `docs/decisions/fm-selection.md`. **RB-010 audit status (2026-09-01): NOT STARTED.** `docs/decisions/fm-selection.md` does not exist.
- [ ] T031 [A:ml-engineer] Fine-tune entrypoint (`src/orbital_drift/train/finetune.py`) per T030; baseline-beats gate encoded in eval step. **RB-010 audit status (2026-09-01): NOT STARTED.** `src/orbital_drift/train/finetune.py` does not exist; no foundation-model code exists anywhere in the repo (confirmed by search — no top-level `train/` directory, no `finetune` module).
- [ ] T032 [HUMAN] Run fine-tune workflow; confirm baseline-beats gate behavior in both directions (force a fail once). Phase gate. **RB-010 audit status (2026-09-01): NOT STARTED**, blocked on T031 + cluster (T003/T005/T012).

## Phase 3 — CT Loop (US4, US5)
- [ ] T033 [A:drift-engineer] Contract tests (failing): drift API, trigger idempotency, hysteresis behavior on synthetic sequences — `tests/contract/test_drift.py`. **RB-010 audit status (2026-09-01): AUTHORED-PROVISIONAL — DONE.** `tests/contract/test_drift.py` exists with 5 tests (verified via `pytest --collect-only` — PSI identical/shifted distributions, multi-band scene evaluation, hysteresis, and coalescing/completion). Task scope met; pending review per RB-010.
- [ ] T034 [A:drift-engineer] Reference-stats builder from training snapshot (`src/orbital_drift/drift/reference.py`). **RB-010 audit status (2026-09-01): NOT STARTED.** `src/orbital_drift/drift/reference.py` does not exist.
- [ ] T035 [A:drift-engineer] Drift metrics via standard lib (PSI/KS per band, prediction-class shift) (`src/orbital_drift/drift/metrics.py`); Prometheus export. No bespoke math (Constitution II). **RB-010 audit status (2026-09-01): AUTHORED-PROVISIONAL — PARTIAL.** PSI (`calculate_psi`) and KS (`scipy.stats.ks_2samp`) are both real; missing prediction-class-distribution shift and Prometheus export — zero `prometheus_client` usage anywhere in `src/` (confirmed by search). Pending review per RB-010.
- [ ] T036 [A:drift-engineer] Trigger emitter with hysteresis window + cooldown + queue-depth-1 coalescing (`src/orbital_drift/drift/trigger.py`). **RB-010 audit status (2026-09-01): AUTHORED-PROVISIONAL — DONE**, but see Part 11 of RB-010: a stuck-breaker defect (`is_retraining_active` has no failure-path reset). **Status corrected 2026-09-05 (RB-012):** that fix LANDED (commit `68eb30a`); this line's "in flight" was stale. Two follow-ons are now recorded instead: the module still consults no config although `drift_hysteresis_window`/`drift_cooldown_scenes` exist and name this file in their own field descriptions (**T061**, D-012 F3); and RB-012's review reports that Part 11's `max_retraining_scenes` staleness net re-dispatches a retrain while one is legitimately still running, because the manager cannot distinguish "the caller forgot to call `mark_retraining_failed()`" from "the job is still going" — reported with a demonstration, unconfirmed here, and to be settled by T057's review of this task. Pending review per RB-010.
- [ ] T037 [A:pipeline-engineer] Drift DAG `dags/drift.py`: post-ingest sensor → compute → export → maybe-trigger; starvation vs shift distinction (spec edge case). **RB-010 audit status (2026-09-01): NOT STARTED.** `dags/` contains only `.gitkeep`.
- [ ] T038 [A:pipeline-engineer] Retrain DAG `dags/retrain.py`: trigger → snapshot → submit Argo train → shadow-eval workflow `workflows/shadow_eval.yaml` → gated promotion (config: auto vs operator-approve). **RB-010 audit status (2026-09-01): NOT STARTED.** `dags/` and `workflows/` contain only `.gitkeep`.
- [ ] T039 [A:runbook-writer] Runbooks: forced retrain, promotion approval, rollback (`docs/runbooks/04-ct-ops.md`, `05-rollback.md`). **RB-010 audit status (2026-09-01): NOT STARTED.** Neither `docs/runbooks/04-ct-ops.md` nor `docs/runbooks/05-rollback.md` exists.
- [ ] T040 [HUMAN] Forced-drift E2E demo: inject shifted scenes, watch trigger → retrain → shadow eval → promotion. Then execute rollback drill; must complete < 10 min (SC-004). Phase gate. **RB-010 audit status (2026-09-01): NOT STARTED**, blocked on T037/T038 + cluster (T003/T005/T012).

## Phase 4 — Serving & Canary (US6)
- [ ] T041 [A:ml-engineer] Contract tests (failing): serving API, stage-loader, canary split — `tests/contract/test_serving.py`. **RB-010 audit status (2026-09-01): AUTHORED-PROVISIONAL — DONE.** `tests/contract/test_serving.py` exists with 2 tests (verified via `pytest --collect-only`: healthz/metrics endpoints, inference with canary routing). Task scope met; pending review per RB-010.
- [ ] T042 [A:ml-engineer] FastAPI serving app (`src/orbital_drift/serve/app.py`): loads by registry stage, canary ratio from config, per-version Prometheus metrics. **RB-010 audit status (2026-09-01): AUTHORED-PROVISIONAL — PARTIAL.** App/`/predict`/canary routing real and tested, but the models are only settable imperatively via `ModelContainer.set_models`, called from no production startup path, and `/metrics` returns hand-rolled JSON (a `dict[str, Any]`), not real Prometheus exposition format. Pending review per RB-010. **Superseded in part 2026-09-05 (RB-012):** this line previously read "the module never imports `orbital_drift.config` (confirmed by search)". It does, at `src/orbital_drift/serve/app.py:21`, since RB-010 Part 5b — the canary ratio now resolves explicit-argument > config > literal fallback. What survives is the startup-wiring gap, and RB-012's review found that Part 13's honest `/healthz` 503 turns that gap into a container which can never report healthy; both are owned by **T053**.
- [ ] T043 [A:infra-scaffolder] Serving deployment manifests/values on the 8GB GPU; readiness/liveness; single-config revert path. **RB-010 audit status (2026-09-01): NOT STARTED.** No serving-specific manifests exist under `infra/` (confirmed by search).
- [ ] T044 [A:runbook-writer] Runbook: canary operations + regression response, `docs/runbooks/06-canary.md`. **RB-010 audit status (2026-09-01): NOT STARTED.** `docs/runbooks/06-canary.md` does not exist.
- [ ] T045 [HUMAN] Deploy serving; demonstrate canary regression alert → revert. Phase gate. **RB-010 audit status (2026-09-01): NOT STARTED**, blocked on T042/T043 + cluster (T003/T005/T012).

## Phase 5 — Observability & Soak (US7, US8)
- [ ] T046 [P] [A:infra-scaffolder] kube-prometheus-stack values + Terraform; Alertmanager routes (DAG failure, drift trigger, canary regression).
- [ ] T047 [P] [A:drift-engineer] Grafana dashboards-as-code: DAG health, Argo states, GPU util/mem/temp, drift series with thresholds, serving per-version — `dashboards/`.
- [ ] T048 [A:runbook-writer] Remaining runbooks: cluster rebuild, GPU-operator recovery, Airflow scheduler failure, lakeFS/SeaweedFS recovery; incident postmortem template; weekly soak-log template — `docs/`.
- [ ] T049 [HUMAN] Deploy observability; verify all three alert classes fire in drills.
- [ ] T050 [HUMAN] (Optional, recommended) Join Tesla P40 as node B; schedule a training job to it; document the heterogeneous-GPU pain in an incident entry. (Deliberate lesson, R-05.)
- [ ] T051 [HUMAN] Rebuild-runbook verification: tear down and rebuild platform once from docs (SC-006).
- [ ] T052 [HUMAN] Run the 6-week soak: weekly logs, incident postmortems, capture ≥1 organic drift retrain. Only the operator may mark this task — and the project — done (Constitution VI).

## Phase 6 — Reconciliation & Integration Hardening (RB-012)

Added 2026-09-05 under RB-012 (`docs/decision-log.md`) after the deep peer review recorded in
`docs/decisions/013-plan-artifact-reconciliation.md`. Every task here owns work that RB-010
**named as a finding and assigned to no part**, or that a later review found in code RB-010
records as already remediated. They exist so the work stops living in prose.

**RB-012 creates these task IDs and their owners; it authorizes the execution of NONE of
them.** Declaring a task is not unlocking it (decision-log rule 4, "decide, then execute").
Each still needs its own authorization and passes the normal gate table before an agent may
start it. T059/T060 are additionally gated on an operator decision that does not yet exist.

- [ ] T053 [A:ml-engineer] Serving startup wiring: build `ModelContainer` from a real composition root so a production model is loaded outside tests, and re-evaluate the Part 12 `HEALTHCHECK` / Part 13 `/healthz` pair together. **Why:** the two parts landed independently and their composition is a container that can never report healthy — `/healthz` returns 503 until `container.production_model` is set, and `set_models()` is called from no production path (`src/orbital_drift/serve/app.py:211-224`; `Dockerfile:87-88`). Under Kubernetes with the same probe as a liveness probe this is an indefinite CrashLoopBackOff on an image whose application code is fine. Invisible to every gate: the docker job builds the image and never runs it. Fix the startup wiring; do **not** revert the honest 503. Includes the port-name defects in the same file (`ORBITAL_DRIFT_SERVE_PORT` matches no config field — `serving_port` reads `ORBITAL_DRIFT_SERVING_PORT`; the exec-form `ENTRYPOINT` hardcodes `--port 8000` so the env var is inert; `config.serving_port` defaults to 8080). Evidence: D-013/02a.
- [ ] T054 [A:spec-implementer] Structured-logging rollout beyond `eval/` and message-path redaction — the deferred half of RB-010 Part 9. **Why:** `configure_logging()` is called from six test modules and from nowhere under `src/` (confirmed by search 2026-09-05), so no handler is installed on the `orbital_drift` logger, records propagate to root, and the `JsonFormatter` that performs redaction never runs in production. Reported additionally: redaction covers `extra=` fields only, while every non-`eval/` module logs by %-interpolation into the message, and `ingest/stac_client.py:190-196` logs `response.text` verbatim. Part 9 was authorized as "logging-redaction fix **+ structured-logging rollout beyond eval/**"; the rollout half was deferred in a commit body with no follow-up entry. Evidence: D-013/02b.
- [ ] T055 [A:ml-engineer] Enforce a real request-body size limit at the ASGI/uvicorn layer. **Why:** `_bound_image_array_size` (`src/orbital_drift/serve/app.py:79-91`) is a pydantic *after*-validator, so the whole body is read and the full nested float list materialised before the bound is evaluated — the allocation it exists to prevent has already happened. The existing test monkeypatches the constant and sends six elements, proving the comparison operator and never the bound. Evidence: D-013/02c.
- [ ] T056 [A:pipeline-engineer] Make the lakeFS simulation honest: deterministic commit ids, and log lines that say SIMULATED. **Why:** `data/lakefs_ops.py:52-64` hashes `time.time()` into the id while its docstring promises determinism (so two ingests of one scene differ and idempotency is impossible), then logs "Created lakeFS commit %s ..." for a commit that does not exist. The fabricated id feeds `train/baseline.py`'s `lakefs_commit_id` — the lineage triple Constitution IV's reproducibility requirement rests on. Highest-risk of the four Critical findings: the only one that destroys the audit trail while emitting affirmative success messages. This task makes the simulation truthful; replacing it is T060. Evidence: D-013/02d.
- [ ] T057 [A:spec-implementer] Run RB-010's retroactive `spec-guardian` + `adversarial-reviewer` review of T013-T052, task by task, and record each outcome. **Why:** RB-010's disposition clause requires this review before any T013-T052 checkbox may flip to `[x]`, but none of its 14 parts *is* this review — the per-part rule covers each remediation part's own diff, not the ~40 pre-existing artifacts. The single activity gating ~40 checkboxes therefore had no part number, no owning agent, no budget line, and no completion criterion. Exit condition: every T013-T052 task carries a recorded review outcome, and the ones that pass are checked off. Evidence: D-013/04a.
- [ ] T058 [A:spec-implementer] Close the `.importlinter` contract hole and give the `architecture` stage a positive control. **Why:** measured against a scratch copy with the pinned `import-linter==2.13`, the current contract set does **not** catch a port importing its own concrete counterpart (`ports/dataversion.py` -> `data.lakefs_ops`: 3 contracts kept, 0 broken), nor `ingest` -> `serve`, nor `ports/compute` -> `drift.trigger`. The `layers` contract lists only `ports` and `domain`, so it forbids exactly one edge. The independent AST re-derivation does not close it either: it compares top-level names only, so every `orbital_drift.*` import collapses to one allowed token. Separately, nothing anywhere asserts `lint-imports` returns non-zero on a real violation — stub-only, which the adversarial-reviewer definition rates a BLOCK. Highest value-per-line item in the review and it touches no production code. Evidence: D-013/05 items 1.
- [ ] T059 [A:ml-engineer] Real MLflow adapter behind `ModelRegistryPort`, converging the two registry implementations. **GATED** on the operator's adapter-disposition decision (roadmap D-2); do not start without it. **Why:** `registry/ops.py` is an in-process dict and `mlflow` is imported nowhere, so FR-006 cannot go green. The measured recommendation is to wrap rather than rewrite — an adapter over an unmodified `ModelRegistryOps`, plus a port-conformance suite parametrized over both implementations — because the two split on merit: the port has the better data model (immutable `LineageEnvelope`, ordered history making rollback exact), `ops.py` the better semantics (validated lifecycle, single-Production invariant, locking) and it is the one the CT loop runs. Evidence: D-013/04b, D-013/04c.
- [ ] T060 [A:pipeline-engineer] Real lakeFS adapter behind `DataVersionPort`. **GATED** on the same operator decision as T059. Depends on T056 (honest simulation first) and on a composition root existing — `get_config()` is called from nowhere in `src/`, there is no factory, and every consumer names a concrete class. Note the self-inflicted blocker to record in the design: RB-010 Part 4 made the lakeFS credentials required with no default, so `get_config()` raises wherever they are absent, which is the stated reason `serve/app.py` never calls it — while the fields those credentials guard are read by nobody. Evidence: D-013/04c.
- [ ] T061 [A:spec-implementer] Wire the five config gaps recorded in `docs/decisions/012-rb010-part6-config-wiring-followups.md` (F1-F5). **Why:** that document is `RECORDED — informational` with no decision-log entry and no task ID — the exact "a note under `docs/decisions/` is reached by no gate and by no reading protocol" failure RB-008a(e) documented and expected to stop recurring. F3 is the priority: `drift/trigger.py`'s `hysteresis_window`/`cooldown_scenes` have config fields that already exist and name that file in their own descriptions. **Charter R-5 watch:** this gap is on its second recorded cycle (RB-010 named it, Part 5 partially fixed it, D-012 re-recorded the remainder); a third recurrence escalates to the operator rather than looping. Evidence: D-013/04e.
- [ ] T062 [A:ml-engineer] Lock `registry/ops.py`'s `rollback_production`, or retract the guarantee its sibling's docstring makes. **Why:** `transition_stage`'s docstring promises unconditionally that two concurrent promotions to Production cannot both succeed; `rollback_production` (`registry/ops.py:185-206`) takes no lock and sets `stage = "Production"`, so an interleaving leaves two versions Production and `get_stage_version` masks it by returning only the highest. RB-010 Part 10 covered two of three read-modify-write sites; the residual is documented in code only. It is also the entry point any rollback drill uses (SC-004). Evidence: D-013/04e.
- [ ] T063 [A:spec-implementer] Fix `eval/calibration.py`'s weight/deviation shape mismatch, which lets `calibration_error` return an ECE above 1. **Why:** `_bin_weights` (`eval/calibration.py:57-78`) re-derives bin occupancy with its own `searchsorted`/`clip`/`bincount` rule, while `sklearn.calibration.calibration_curve` derives populated bins by a different one. When they disagree, the elementwise product at `calibration_error:113-114` does not raise — numpy broadcasts the shorter array. Reproduced at HEAD: `labels=[False,False,False]`, `probabilities=[1.0,0.5,0.5]`, `bin_count=2`, `strategy="quantile"` gives 1 weight against 2 deviations and **ECE = 1.5**, for a quantity bounded in `[0,1]` by construction. The weights sum to 1.0, so the obvious sanity check does not catch it; the missing shape assertion is the defect. **Two consequences beyond the wrong number:** (a) `eval/calibration.py` is one of the three modules RB-010 certified as Constitution II compliant on the strength of its sklearn import — the hand-rolled post-processing was never assessed; (b) `tests/unit/test_eval_calibration.py::test_ece_is_bounded_for_equal_length_binary_inputs` is a Hypothesis test with `max_examples=20`, so it reddens CI **intermittently** on unrelated PRs. It was green during the RB-011 triage (2026-09-03) and failed on the RB-012 run, so this is draw luck, not a new regression. Found by running the suite, not by review. Evidence and reproduction: D-013/02e. Coordinate with the D-011 Principle II decision before changing eval mathematics.
- [ ] T064 [A:spec-implementer] Implement the status half of the governance-harness scenario "Trace cites a nonexistent task", or amend that requirement under its own authorization. **Why:** the scenario (`openspec/changes/adopt-governance-kit/specs/governance-harness/spec.md`) requires the consistency test to fail when a story's "status contradicts the checkbox state". `Story` carries no status field and no test asserts one, so that half is implemented by nothing. `roadmap_data.py` and `tests/unit/test_projections.py` both CLAIMED it was checked; RB-012 removed the false claim, and this task exists so that removal does not convert a specified requirement into an invisible gap (the RB-008a(e) failure mode). Found by spec-guardian and adversarial-reviewer independently on the RB-012 diff. Evidence: D-013/08 finding M6/6.
- [ ] T065 [A:spec-implementer] Bring `docs/development/**` under governance, or delete the directory and fold its content into the governed documents. **Why:** RB-012's own root-cause analysis found these two files rotted because the directory is named by no task ID, omitted from RB-010 Part 1's reconciliation scope, absent from every CHANGELOG entry, and outside `pyproject.toml`'s `governed_path_globs` while `tests/governance/test_governance_meta.py` allowlists `docs/*` as public plumbing. RB-012 repaired the CONTENT and left the CAUSE in place; without this task the same drift recurs, and a diagnosed-but-unowned cause is what RB-008a(d)/(e) require to be named so it cannot become permanent by silence. Evidence: D-013/08 finding 7.

## Dependencies (summary)
Phase 0: T001 → T002 → **[T004, T006–T010 may proceed in parallel with the T003/T005 hardware gates]** → T011 → T012. The `[HUMAN]` gates T003 and T005 block *execution*; authoring may proceed alongside them. T011 still requires T006–T010 to be review-APPROVED (not merely drafted), since it documents bring-up order for the final artifacts. T012 requires everything. Operator-approved 2026-08-09.

⚠ **T006, once authored, is AUTHORED-PROVISIONAL until re-reviewed against the T003/T005 verification blocks** (no T006 artifact exists yet; per RB-007 its authoring is deferred until a `G-1` entry exists in `docs/decision-log.md`, since three of its four hardware couplings resolve only at T003). An earlier revision of this section claimed "no agent artifact in T004 or T006–T010 depends on the cluster existing." **That was wrong** — `docs/decisions/000-phase0-technical-decisions.md` records four couplings to T003, three of which land directly in T006's GPU Operator values:

| Coupling | Resolved at | Lands in |
|---|---|---|
| GPU UUIDs (`nvidia-smi -L`) | T003 | T006/T010 workload manifests |
| DCGM field support on consumer Blackwell (D-000/D-11) | T003 (`dcgmi dmon -e 203,252,150`) | T006 `dcgmExporter` counters CSV |
| `RUNTIME_CONFIG_SOURCE=file` on k3s + containerd 2.2.5-k3s2 (D-000/D-02) | T005 | T006 `toolkit.env` |
| GPU Operator 26.3.3 vs driver branch 610 (validated list stops at 595.71.05) | T003 | T006 chart pin viability |

**T006 must therefore be re-reviewed against T003's and T005's verification blocks before T011 may cite it.** T004 and T007–T010 carry no GPU coupling and are unconditionally parallel.

All host-specific values are parameterised per Principle III and D-10 — GPU UUIDs use the names already defined in `.env.example`: `ORBITAL_DRIFT_TRAIN_GPU_UUID` and `ORBITAL_DRIFT_SERVE_GPU_UUID`. Never literals; the repo is public.

(Original text: "Phase 0 strictly ordered T001→T012 except [P] block T006–T010.") Phase 1: T013–T015 before T016+; T020 needs T016–T019. Phase 2: T023–T025 before T026; T031 needs T029+T030. Phase 3: T033 before T034–T036; T038 needs T036+T027. Phase 4 needs Phase 2. Phase 5 soak (T052) needs all gates passed.
