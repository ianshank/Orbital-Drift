# Verified Version Pins

**Retrieval date: 2026-08-08. Re-verified 2026-08-22 (apply-readiness).** Every version below was confirmed against a live source in-session, per the plan's Step 3 pinning protocol. A pin without a provenance URL is a `spec-guardian` BLOCK. Anything marked UNVERIFIED must not be pinned until confirmed.

Re-verify before use if this file is more than ~30 days old. `kube-prometheus-stack` in particular releases very frequently.

**2026-08-22 re-verification note (RB-007(c)).** The ~30-day window had **not** lapsed — only 14 days since 2026-08-08. This refresh is an **apply-readiness** re-verification ahead of T012 (`terraform apply`) and T006's post-G-1 GPU Operator authoring, not a staleness-compliance pass — stated per the program peer-review's staleness-honesty finding (a Minor delivered in-session 2026-08-22; recorded durably only in summary at `docs/decisions/009-program-review-record.md` D-009/01, which notes six Minors without enumerating them). Method and honest limits: several original provenance domains are egress-blocked from the re-check environment (`update.k3s.io`, `download.nvidia.com`, `docs.nvidia.com`, `artifacthub.io`, `airflow.apache.org`, `charts.min.io`, `seaweedfs.github.io`, `mlflow.org`; `api.github.com` returns 403 through the proxy). Where a row's original URL was unreachable, it was re-verified against an equivalent authoritative source (tag-pinned `raw.githubusercontent.com` Chart.yaml or the project's GitHub releases page), noted per row; rows with **no** reachable equivalent are marked UNVERIFIED-ON-RECHECK below rather than silently keeping the old confirmation date. **No pin in this file or in merged `infra/**` was changed by this refresh** — observed upstream drift is *recorded* (see "2026-08-22 re-check findings"), and any pin bump is its own governed change. A same-day cross-check found zero mismatches between this file and the merged Terraform **chart** pins (`lakefs 1.12.24`, `argo-workflows 1.0.23`, `airflow 1.22.0`, `mlflow 1.11.4`, `cloudnative-pg 0.29.0`, `cluster 0.8.1`, `seaweedfs 4.41.0`) — the claim is scoped to chart pins; the four Terraform **provider** pins (`helm 3.2.0`, `kubernetes 3.2.1` in each stage's `versions.tf`) have no row in this file, and were separately verified at review (adversarial pass, 2026-08-22: provider tags `v3.2.0`/`v3.2.1` exist upstream; helm `v3.2.1` does not, so the asymmetric pinning is correct).

## Host / cluster

| Component | Version | Source | Re-verified 2026-08-22 |
|---|---|---|---|
| k3s (recommended) | `v1.35.7+k3s1` | https://update.k3s.io/v1-release/channels · https://docs.k3s.io/release-notes/v1.35.X | CONFIRMED — still the newest v1.35.x stable (channels API egress-blocked on re-check; confirmed via https://github.com/k3s-io/k3s/releases, which also shows `v1.35.8-rc1+k3s1` as a 2026-08-21 pre-release only) |
| k3s (latest stable — **not** recommended, see D-07) | `v1.36.3+k3s1` | https://update.k3s.io/v1-release/channels | CONFIRMED — marked "Latest" on https://github.com/k3s-io/k3s/releases (channels API egress-blocked); `v1.36.4-rc1+k3s1` exists as pre-release only |
| NVIDIA driver (host, **open** modules) | `610.57.04` | https://download.nvidia.com/XFree86/Linux-x86_64/ | UNVERIFIED-ON-RECHECK — `download.nvidia.com` egress-blocked, no reachable equivalent; 2026-08-08 confirmation stands as last-known-good |
| CUDA | `13.x` | https://docs.nvidia.com/datacenter/tesla/tesla-release-notes-610-57-04/index.html | UNVERIFIED-ON-RECHECK — `docs.nvidia.com` egress-blocked; 2026-08-08 confirmation stands as last-known-good |
| GPU Operator chart | `v26.3.3` | https://docs.nvidia.com/datacenter/cloud-native/gpu-operator/latest/getting-started.html | DRIFTED — upstream latest is now `v26.7.0` (released 2026-08-21, per https://github.com/NVIDIA/gpu-operator/releases); `v26.3.3` remains the newest 26.3.x patch. See finding 1 below — T006-relevant |

RTX 5060 Ti (device ID `2D04`) and RTX 5060 (`2D05`) are both explicitly listed as supported by driver 610.57.04: https://download.nvidia.com/XFree86/Linux-x86_64/610.57.04/README/supportedchips.html *(2026-08-22: UNVERIFIED-ON-RECHECK — `download.nvidia.com` egress-blocked; 2026-08-08 confirmation stands as last-known-good.)*

## Platform charts

| Component | Chart repo | Chart | App | Source | Re-verified 2026-08-22 |
|---|---|---|---|---|---|
| Apache Airflow (official) | `https://airflow.apache.org` | `1.22.0` | `3.2.2` | https://github.com/apache/airflow/releases | CONFIRMED — chart `1.22.0` (2026-06-13) is still the newest Helm chart release; its `appVersion: 3.2.2` re-confirmed at the tag-pinned https://raw.githubusercontent.com/apache/airflow/helm-chart/1.22.0/chart/Chart.yaml. Core Airflow has moved to `3.3.1` with no newer chart yet — see finding 4 |
| Argo Workflows | `https://argoproj.github.io/argo-helm` | `1.0.23` | `v4.0.8` | https://raw.githubusercontent.com/argoproj/argo-helm/main/charts/argo-workflows/Chart.yaml | PIN CONFIRMED, UPSTREAM DRIFTED — the pinned tag is intact (`1.0.23`/`v4.0.8` at https://raw.githubusercontent.com/argoproj/argo-helm/argo-workflows-1.0.23/charts/argo-workflows/Chart.yaml); `main` is now `2.0.2`/`v4.1.2`, past the `2.0.0`/`v4.1.0` D-005/08 flagged. OPERATOR decision — see finding 2 |
| **MLflow (CHOSEN — community)** | `https://community-charts.github.io/helm-charts` | `1.11.4` | `3.15.1` | https://artifacthub.io/api/v1/packages/helm/community-charts/mlflow | PIN CONFIRMED, UPSTREAM DRIFTED — pinned tag intact (`1.11.4`/`3.15.1` at https://raw.githubusercontent.com/community-charts/helm-charts/mlflow-1.11.4/charts/mlflow/Chart.yaml); `main` is now chart `1.11.5`, same app `3.15.1` (artifacthub.io egress-blocked; source repo used). See finding 3 |
| MLflow (official — future migration, D-05b) | `oci://ghcr.io/mlflow/charts/mlflow` | `0.1.0` | `3.15.1` | https://mlflow.org/docs/latest/self-hosting/kubernetes-helm/ | UNVERIFIED-ON-RECHECK — `mlflow.org` egress-blocked; `github.com/mlflow/charts` and `mlflow/mlflow:/charts/` both 404, and the OCI registry is not fetchable from this environment. Not consumed by any merged infra file (future-migration row only) |
| lakeFS | `https://charts.lakefs.io` | `1.12.24` | `1.103.0` | https://raw.githubusercontent.com/treeverse/charts/lakefs-1.12.24/charts/lakefs/Chart.yaml | PIN CONFIRMED, UPSTREAM DRIFTED — pinned tag intact (`1.12.24`/`1.103.0`); `master` is now `1.12.25`/`1.104.0` (https://raw.githubusercontent.com/treeverse/charts/master/charts/lakefs/Chart.yaml). Second consecutive patch drift for this chart — see finding 3 |
| **SeaweedFS (CHOSEN — object store)** | `https://seaweedfs.github.io/seaweedfs/helm` | `4.41.0` | `4.41` | https://artifacthub.io/api/v1/packages/helm/seaweedfs/seaweedfs | PIN CONFIRMED, UPSTREAM DRIFTED — pinned source intact (`4.41.0`/`4.41` at https://raw.githubusercontent.com/seaweedfs/seaweedfs/4.41/k8s/charts/seaweedfs/Chart.yaml); `master` is now `4.44.0`/`4.44`. Honest limit: both artifacthub.io and the published repo index (`seaweedfs.github.io`) are egress-blocked, so the *published* index could not be re-read — drift and pin-existence verified against the chart's source repo at tag/master. See finding 3 |
| MinIO (REJECTED — upstream archived, D-05) | `https://charts.min.io` | `5.4.0` | `RELEASE.2024-12-18T13-15-44Z` | https://charts.min.io/index.yaml | CONFIRMED (moot — REJECTED, never adopted) — `charts.min.io` egress-blocked; `5.4.0`/`RELEASE.2024-12-18T13-15-44Z` re-confirmed via https://raw.githubusercontent.com/minio/minio/master/helm/minio/Chart.yaml |
| CloudNativePG operator | `https://cloudnative-pg.github.io/charts` | `0.29.0` | `1.30.0` | https://api.github.com/repos/cloudnative-pg/charts/releases | CONFIRMED — `cloudnative-pg-v0.29.0` still the newest operator-chart release (api.github.com returns 403 through the re-check proxy; verified via the HTML page https://github.com/cloudnative-pg/charts/releases) |
| CNPG `cluster` chart | same | `0.8.1` | — | same | CONFIRMED — `cluster-v0.8.1` still newest (same fallback source) |
| CNPG `plugin-barman-cloud` | same | `0.7.1` | — | same | CONFIRMED — `plugin-barman-cloud-v0.7.1` still newest (same fallback source) |
| kube-prometheus-stack (Phase 5) | `https://prometheus-community.github.io/helm-charts` | `88.2.0` | `v0.93.0` | https://artifacthub.io/api/v1/packages/helm/prometheus-community/kube-prometheus-stack | DRIFTED — `main` is now `88.5.3`/`v0.93.1` (artifacthub.io egress-blocked; verified via https://raw.githubusercontent.com/prometheus-community/helm-charts/main/charts/kube-prometheus-stack/Chart.yaml). Exactly the fast-mover this file self-declared; not yet consumed by any merged infra file (Phase 5). See finding 5 |
| prometheus-operator-crds (CRD stage) | same | `31.0.0` | `v0.93.0` | https://artifacthub.io/api/v1/packages/helm/prometheus-community/prometheus-operator-crds | DRIFTED — `main` is now `31.0.1`/`v0.93.1` (same fallback method, https://raw.githubusercontent.com/prometheus-community/helm-charts/main/charts/prometheus-operator-crds/Chart.yaml). Not yet consumed by a merged infra file. See finding 5 |

## Rejected / not viable

- **Bitnami PostgreSQL** — chart `18.8.7` is published but its `18.4.0` image is unpullable. The public `docker.io/bitnami` catalog was deleted 2025-09-29; `bitnamilegacy/postgresql` is frozen at `17.6.0` (Aug 2025, no patches); the free tier is `latest`-only with no version pinning, which is incompatible with Principle IV. Source: https://github.com/bitnami/charts/issues/35164 · https://vcf.broadcom.com/vsc/bitnamiAnnouncement.pdf
## Corrections to earlier drafts of this file

- **`CONTAINERD_RUNTIME_CLASS` still exists** in toolkit v1.19.1 (flag `runtime-name`, aliases `runtime-class`; env sources `NVIDIA_RUNTIME_NAME`, `CONTAINERD_RUNTIME_CLASS`, `DOCKER_RUNTIME_NAME`) despite being absent from the v26.3.3 options table. Its default `nvidia` already matches k3s's handler name — **do not override it**, but it is not obsolete as an earlier draft claimed.
- **`CONTAINERD_CONFIG` and `CONTAINERD_SOCKET` are required** and must point at k3s's paths. An earlier draft claimed the NRI path made them unnecessary; that was wrong (see D-02).
- **lakeFS chart pin corrected `1.12.22` → `1.12.24`** (appVersion `1.102.0` → `1.103.0`). T007 (`docs/decisions/002-infra-layout.md` D-11, Follow-up #6) re-verified 2026-08-16 directly against the tag-pinned source (`.../treeverse/charts/lakefs-1.12.24/charts/lakefs/Chart.yaml`, three independent fetches agreeing) rather than `master`, closing the residual risk D-11 flagged when it first found the drift. The three `existingSecret`-key-path findings D-002 Follow-up #6 also references (CloudNativePG, community MLflow, SeaweedFS) are recorded in `docs/decisions/002-infra-layout.md` D-08–D-10 and `docs/decisions/000-phase0-technical-decisions.md` D-09; the UNVERIFIED bullet below is now edited to reflect that (peer-reviewer finding on the integrated T007-T010 set, `docs/decisions/006-t007-t010-integration.md`) — kube-prometheus-stack remains genuinely unresolved (Phase 5, out of T007-T010's scope), and MinIO's entry is moot since it was rejected and never adopted.

## 2026-08-22 re-check findings (apply-readiness refresh, RB-007(c))

Drift is *recorded* here, never enacted: no pin in this file or in merged `infra/**` was changed. A pin bump is its own governed change (pin-a-tool skill), separately reviewed and cited.

1. **GPU Operator drifted `v26.3.3` → `v26.7.0`** (released 2026-08-21 per https://github.com/NVIDIA/gpu-operator/releases; `v26.3.3` is still the newest 26.3.x patch). **T006 consumes this pin after G-1.** The driver-branch compatibility question this file already carries (validated list stops at `595.71.05`; behavior on branch 610 unconfirmed) could **not** be re-checked for either version: `docs.nvidia.com` is egress-blocked from the re-check environment and the GitHub release body for `v26.7.0` failed to render. Whether `v26.7.0` extends the validated driver list to 610.x is therefore **unknown**. Before T006 authoring, the operator should decide `v26.3.3` (as pinned) vs `v26.7.0` — with the 26.7 release notes (https://docs.nvidia.com/datacenter/cloud-native/gpu-operator/26.7/release-notes.html) read from a network that can reach them. An agent must not make this call.
2. **Argo Workflows major drift has advanced: upstream `main` is now chart `2.0.2` / app `v4.1.2`**, past the `2.0.0`/`v4.1.0` D-005/08 first flagged. The merged pin (`infra/terraform/20-platform/argo_workflows.tf`, chart `1.0.23`/`v4.0.8`) is confirmed intact and fetchable at its tag. **Decision path: staying at `1.0.23`/`v4.0.8` vs commissioning a decision doc for the major bump is an OPERATOR call — an agent may not make it** (D-005/08's own conclusion, restated by `docs/decisions/005-t010-argo-workflows.md` follow-up #5). A major bump can carry CRD-schema, RBAC value-path, and behavior changes nobody has researched; until a decision doc exists, `1.0.23` stands.
3. **Patch drift on three CHOSEN charts, pins still valid:** lakeFS `1.12.24` → `1.12.25` (app `1.104.0`) — the chart's *second* consecutive drift (`1.12.22` → `1.12.24` → `1.12.25`), consistent with its release cadence; community MLflow `1.11.4` → `1.11.5` (app unchanged at `3.15.1`); SeaweedFS `4.41.0` → `4.44.0` (app `4.44`). In all three cases the *pinned* version was re-confirmed to exist at its tag-pinned source, so T012's `terraform apply` will still resolve every merged pin. No bump enacted.
4. **Airflow: app-level movement, no chart drift.** Core Airflow `3.3.1` is out (2026-08-12), but the newest Helm chart is still `1.22.0` with `appVersion: 3.2.2` — the merged pin is current at chart level. Nothing to decide until upstream cuts a chart carrying 3.3.x.
5. **Observability pair drifted (Phase 5, not yet consumed):** kube-prometheus-stack `88.2.0`/`v0.93.0` → `88.5.3`/`v0.93.1`; prometheus-operator-crds `31.0.0`/`v0.93.0` → `31.0.1`/`v0.93.1`. Neither is pinned in any merged infra file yet (the `00-crds` stage currently holds only CNPG; the prometheus-operator-crds placement is still plan-level, D-000). Given this chart family's self-declared cadence, **re-verify at T046/Phase-5 authoring time rather than trusting today's numbers**. Its `existingSecret` key names remain genuinely unresolved (unchanged claim, per D-006's reconciliation).
6. **Egress limits of this re-check, stated once:** `update.k3s.io`, `download.nvidia.com`, `docs.nvidia.com`, `artifacthub.io`, `airflow.apache.org`, `charts.min.io`, `seaweedfs.github.io`, and `mlflow.org` are blocked by the re-check environment's egress proxy, and `api.github.com` answers 403 through it. Every row's per-row note names the fallback source actually used; rows with no reachable equivalent are UNVERIFIED-ON-RECHECK, not silently re-dated.

## UNVERIFIED — do not pin without confirming

- Garage official chart version (chart lives on a Gitea instance that could not be retrieved; the artifacthub `2.3.3` entry is a third-party mirror, not official)
- Zalando postgres-operator chart version
- `existingSecret` key names for kube-prometheus-stack (still genuinely unresolved — out of scope for T007-T010, belongs to T046/Phase 5)
- ~~`existingSecret` key names for CloudNativePG, community MLflow, SeaweedFS~~ — RESOLVED. CloudNativePG: `docs/decisions/002-infra-layout.md` D-08, independently re-confirmed at the source-code level by `docs/decisions/004-t009-airflow-findings.md` Finding 1. Community MLflow: D-09, corrected in place after the originally-relayed path was found wrong (`docs/decisions/003-t008-mlflow-secret-wiring-findings.md` D-003/01). SeaweedFS: D-10, corrected to `s3.existingConfigSecret` (not `s3.auth.existingSecret` as originally relayed); the *outer* field path is confirmed against the chart's own `values.yaml`, and the *inner* `seaweedfs_s3_config` JSON payload shape is confirmed against the chart's own `README.md` worked example (a peer-reviewer finding on the integrated T007-T010 set caught that this line originally overstated what had been checked — only the outer path, not the inner shape — before that second citation was added; both are now in `infra/terraform/10-storage/seaweedfs.tf`'s header comment). Residual, stated honestly: this is the chart maintainers' documented contract, not SeaweedFS core's own IAM-loader source — the practical ceiling on static verification, closed the rest of the way only by a live T012 smoke test.
- ~~`existingSecret` key names for MinIO~~ — moot; MinIO is REJECTED (D-05, upstream archived), never adopted.
- GPU Operator `v26.3.3` exact release date (sources disagreed) — *2026-08-22: the GitHub releases page shows `v26.3.3` dated 25 Jun; the original disagreement with docs.nvidia.com could not be re-arbitrated (`docs.nvidia.com` egress-blocked)*
- Whether GPU Operator 26.3.3 functions on driver branch 610 (validated list stops at 595.71.05) — *2026-08-22: still open, UNVERIFIED-ON-RECHECK (`docs.nvidia.com` egress-blocked; the `v26.7.0` GitHub release body failed to render). See re-check finding 1: this now also applies to `v26.7.0`, and resolving it is part of the operator's pre-T006 version decision*
