# Decision Log — Orbital-Drift
Format: YYYY-MM-DD | ID (DEC-00x / RB-xxx[a] / G-x) | decision | recorded-by
<!-- Gates read THIS file. An empty log means: only Day-0 + M0 tasks are unlocked. -->

> RULES — the conventions that make this file load-bearing
>
> 1. **One entry per line, fixed format.** Gates and tests presence-check IDs in this
>    file; prose elsewhere (chat, PR descriptions, commit messages) unlocks NOTHING.
>
> 2. **Entry types:**
>    - `DEC-00x` — a CONFIRM-FIRST decision from charter §5 is confirmed (or overridden).
>    - `RB-xxx`  — a process/re-baseline decision: PR batch authorizations, scope
>      corrections, policy amendments. Number sequentially, never reuse.
>    - `RB-xxxa` — an EXECUTION RECORD under a prior decision, not a new decision. Say so
>      explicitly in the entry text.
>    - `G-x` — human gates (operator executed a [HUMAN] task, sign-off given).
>
> 3. **Namespace rule (design D7).** This file owns `DEC-`/`RB-`/`G-` IDs only. ADR-style
>    rationale documents keep their `D-nn` ids under `docs/decisions/` (e.g. `D-000/D-05`)
>    and plan.md keeps `D-01…D-05`; the prefixes are disjoint, so an ID is unambiguous.
>    A log line decides; an ADR explains.
>
> 4. **Decide, then execute.** A decision is logged as its own line; the PR that executes
>    it is separately costed and cites the line.
>
> 5. **State what an entry does NOT do.** If a decision doesn't unlock gated work, end
>    the entry with it.
>
> 6. **Authorization ordering.** Owner approval given in-session BEFORE work starts may
>    be logged after, landing with the commits it authorizes — but the entry must say so.
>
> 7. **Never spoof a gate.** A transmission/progress record related to a gate must NOT
>    carry the gate's ID.

2026-08-21 | DEC-001 | Adopt the SSD governance kit as OpenSpec change adopt-governance-kit; Constitution amended to v1.1.0 (Principle II scoped to code/metrics/eval-harness logic; governance/process artifacts exempt). Enforcement: spec-guardian text updated; kit lands via phased PRs each leaving ci/checks.sh green (design D1–D14). Owner approval given in-session via the approved import plan BEFORE work started; logged after per rule 6 | Ian (operator, in-session plan approval; recorded by agent)
2026-08-21 | DEC-002 | Milestone budgets: max 4 PRs and 16 engineering-hours per milestone before mandatory owner review (charter §6). Confirmed as the approved plan's default. NOT a gate unlock — [HUMAN] tasks and their G-x entries still gate execution phases | Ian (operator, in-session plan approval; recorded by agent)
2026-08-21 | DEC-003 | Remote allowlist = this repo's origin remote only, recorded in .claude/allowed-remotes.txt when Phase-6 lands the guard scripts that read it. Confirmed as the approved plan's default. Unlocks nothing until those scripts exist | Ian (operator, in-session plan approval; recorded by agent)
2026-08-21 | DEC-004 | Coverage floor fail_under=90 over src/orbital_drift only, armed with the cov stage. HONEST LIMIT: until T013+ land real feature code, src is docstring stubs plus governance modules, so the floor measures only those modules and has near-zero enforcement value; re-setting the floor requires a new DEC entry | Ian (operator, in-session plan approval; recorded by agent)
2026-08-21 | RB-001 | Agent roster change under DEC-001/design D5: spec-implementer added as default implementer; adversarial-reviewer supersedes peer-reviewer (peer-reviewer.md deleted, its domain heuristics carried into the new agent); CLAUDE.md protocol gains step 0 (gate check) and the R-5 fix-cycle cap. EXPLICIT LIMIT: authorizes the roster/protocol text changes only; it unlocks no feature task | Ian (operator, via approved plan; recorded by agent)
2026-08-21 | RB-002 | Tooling change under DEC-001/DEC-003 (skill Tooling-changes rule): .claude/settings.json gains the PreToolUse guard hook (scripts/pretooluse_guard.sh, exit 2 blocks), a SessionStart version-check (never an install, always exit 0), and allow entries for the make/checks.sh gate commands; all 36 Principle-I deny entries kept verbatim; extraKnownMarketplaces omitted (no third-party plugin sources). .claude/allowed-remotes.txt seeded with origin per DEC-003; native pre-push hook installable via scripts/install_hooks.sh. EXPLICIT LIMIT: unlocks no feature task | Ian (operator, via approved plan; recorded by agent)
2026-08-21 | RB-002a | EXECUTION RECORD under DEC-001/RB-001/RB-002, not a new decision: adopt-governance-kit tasks 0.1-6.1 executed across six gate-green commits (constitution v1.1.0; control plane; harness stages dead/audit/specs/cov/traceability/projections + zero-skip guard; agent roster; traceability + meta-tests; guards + settings merge + projections). Every new gate red-checked; final local gate: thirteen stages green, coverage 97.34%, 341 tests. Tasks 6.2 (push/CI) and 6.3 (T002) complete separately | agent (execution of the already-approved decisions; no new owner decision is claimed or implied)
2026-08-21 | RB-003 | Authorize a governance-hardening PR under DEC-001 following a three-dimension audit (code hygiene, test/coverage QA, wiring/tech-debt). Scope: fix the PreToolUse guard's measured fail-open bypasses by moving tokenization from sed/ERE into orbital_drift.guard (shlex); anchor the zero-skip capability-guard allowance to a prefix; add a `governance` checks.sh stage so meta-test failures redden a job named for them; give stage_cov the docker/git preflight stage_unit has; raise the coverage floor to 95 and add a per-file floor (orbital_drift.covcheck); harden traceability/projections/roadmap_data against the measured edge cases; extract scripts/_lib.sh. EXPLICIT LIMIT: no feature task is unlocked, and no charter constraint or gate THRESHOLD is relaxed - the coverage bar rises and the guard gets stricter | Ian (operator, hardening request; recorded by agent)
2026-08-21 | RB-004 | Tooling change under RB-003 (skill Tooling-changes rule): .claude/settings.json hook commands use ${CLAUDE_PROJECT_DIR:-.} so the scripts' own fallback is reachable (a bare unset variable made bash exit 127, which Claude Code treats as a non-blocking error and RUNS the tool); permissions.allow adds the sh/bash forms actually used on a make-less box; deny-list gains the missing ` *` spellings for kustomize build and six terraform verbs. All prior deny entries retained. EXPLICIT LIMIT: unlocks nothing | Ian (operator, via RB-003; recorded by agent)
2026-08-21 | RB-005 | Authorize a documentation/validation hardening batch: CHANGELOG.md, docs/architecture/ARCHITECTURE.md (C4 context+container reflecting actual Phase-0 build state vs planned Phase 1-5 containers), README gate-list refresh (governance/cov/traceability/projections were missing), a deterministic structural-validation suite for .claude/agents and .claude/skills (frontmatter completeness, roster cross-reference, no dangling references), and a behavioral test for scripts/session_start_check.sh (previously covered only by an existence check). EXPLICIT LIMIT: documentation and test coverage only; no gate threshold changes, no new task unlocked, no charter/constitution edit | Ian (operator, hardening request; recorded by agent)
2026-08-21 | G-0 | T001 verified done (commit d916665; six-stage CI green). Per the tasks.md dependency note (operator-approved 2026-08-09), Phase-0 authoring tasks T002, T004, T006–T011 are unlocked; [HUMAN] tasks T003/T005/T012 remain operator-only and no later phase is unlocked | Ian (operator; recorded by agent)
2026-08-21 | RB-006 | Reconciliation of 001-orbital-drift-ct (governance-kit import, d916665..2933bc3) with origin/main's independently-merged PR #3 (34 commits: T007-T010 infra fan-out, T001a coverage gate, T001b terraform-fmt hook, T002/T004 runbooks) — both lines diverged from d916665 with no visibility into each other. Strategy: squash this branch to af1c1cd, cherry-pick onto origin/main (10ae855) as reconcile-001, resolve 9 real conflicts plus defects an unattended auto-merge left unflagged (a duplicate-kwarg SyntaxError in tests/unit/shell_harness.py that would have crashed test collection; a stale Makefile target; dangling peer-reviewer references in two skills). Key judgment calls: (1) origin/main's stage_coverage (FR-011a, COVERAGE_MIN_PERCENT=85, three reviewed diagnostic fixes D-11/D-12/D-13) kept as the sole global-floor mechanism; this branch's competing cov stage dropped; this branch's per-file floor (orbital_drift.covcheck) retained, wired into stage_coverage's success path instead of its own stage; pyproject.toml's [tool.coverage] section removed per origin/main's own guard test (test_no_coverage_config_silently_redefines_what_the_gate_measures). DEC-004's 90/95 coverage-floor proposal is superseded for the global floor — origin/main's operator-ratified 85 (D-05) stands; the per-file 90 floor survives as an independent, additive check. (2) origin/main's docs/runbooks/00-host-prep.md kept as canonical over this branch's independently-authored version of the same T002 runbook, since origin/main's is cross-referenced by T004/T004a/T007-T010 infra that exists only on that line — keeping this branch's copy would have orphaned it. (3) peer-reviewer.md deletion (RB-001) stands; the one real finding origin/main added to it since (new CI gates need a stub test AND a positive control; "stub-only is a BLOCK") was ported into adversarial-reviewer.md before deletion so it isn't silently lost. Verified: sh ci/checks.sh all green on two independent full runs (627 tests, 96-97% coverage, zero conflict markers, gitleaks clean over full history) before any commit. EXPLICIT LIMIT: reconciles two lines of work; unlocks no new feature task; does not relax any gate — the ratified global coverage floor is origin/main's 85 (D-05), not this branch's proposed value | Ian (operator, rebase-strategy approved in-session; recorded by agent)
2026-08-22 | RB-007 | Phase-0 completion program authorized, three parts. (a) UNLOCK EXTENSION: G-0 enumerates T002, T004, T006–T011 by ID; T004a (split out of T004 on PR #3 review) and T001b (added on operator request, Phase-0 plan step 7) postdate that list and are unlocked by no logged entry — the Phase-0 authoring unlock is extended to T004a and T001b, and the governance skill's gate-table row is amended in this same change (two-file rule). (b) BUDGET BASELINE: DEC-002's 4-PR/16-hour M0 counter is read as resetting at RB-006's owner-approved reconciliation; counted from there the planned feature PRs are T004a, T001b-closure, T006 (deferred until after G-1 by operator choice — three of its four hardware couplings resolve only at T003), and T011 = 4 of 4, putting the mandatory owner review at the natural G-2 boundary; estimated effort ≤ 12 h. (c) PROCESS BATCH: process/docs PRs outside the M0 budget are authorized for exactly: stale-status refresh (README, ARCHITECTURE.md, CHANGELOG.md, governance-skill summary lines), decision-doc reconciliation (docs/decisions/002–006 status headers with citations, 005's renumbering prose, operator-ratification checklist), versions.md apply-readiness re-verification, and the tasks.md dependency-note wording fix (present-tense AUTHORED-PROVISIONAL claim). Operator approval given in-session 2026-08-22 BEFORE work started; logged after per rule 6, landing with the commits it authorizes. EXPLICIT LIMIT: unlocks no Phase 1+ task — T013+ stays locked on G-1/G-2/G-3, which only the operator can create; changes no gate threshold; doc 007's review remains owned by the T001b-closure PR, not the process batch | Ian (operator, in-session approval; recorded by agent)
2026-08-22 | RB-007a | EXECUTION RECORD under RB-007, not a new decision. spec-guardian round-1 review of the process batch returned one Major: the D-002/D-03 helm-provider example correction (and 005 Follow-up #6's resolution mark) exceed RB-007(c)'s literal enumeration. Disposition: the operator's in-session approval of the program (2026-08-22, same approval RB-007 records) explicitly included that correction as a named batch item (program peer-review finding F-9b: "fix D-002's stale helm-provider block-syntax example flagged in 005"); recorded here per rule 6 rather than re-asked. The correction stands; 005 Follow-up #6 and 006 Follow-up #2 are annotated resolved in lockstep. Also making RB-007(c)'s remaining deliverables explicit per the guardian's finding 2: the operator-ratification checklist is delivered in the batch PR's description (PR #5) — the durable per-doc state stays in each doc's own status header, which only the operator may change; versions.md apply-readiness re-verification is deliberately deferred to its own PR under the same RB-007(c) authorization. EXPLICIT LIMIT: records execution of already-approved scope; unlocks nothing beyond RB-007 | Ian (operator, in-session program approval; recorded by agent)
2026-08-22 | RB-008 | Code-hygiene and gate-integrity program authorized after a four-stream audit (CI/CD triage, dead/redundant code, hardcoded values/modularity, branch coverage) the operator requested in-session 2026-08-22; logged BEFORE execution per rule 4 (decide, then execute). Batch = three process-track PRs. (1) GATE INTEGRITY: ci/validate_specs.sh fails closed instead of exit-0 on a missing or empty openspec/changes (scripts/_lib.sh:15-17 names this very file as the measured instance of the cwd-relative-literal bug, and the literal is still there); preflight() verifies the pinned interpreter for a stage that executes Python but declares no tool pins — projections is the only one, since ci/checks.sh:559-561 returns before require_python_interpreter:586 while ci/checks.sh:2142 runs the module; .github/workflows/ci.yml limits cancel-in-progress to pull_request so a push to main is never cancelled unscanned (run 32575049407 on merge commit 4a76930 was cancelled 43s in, leaving that commit with no completed gitleaks full-history scan, and `cancelled` never reads as failure — Constitution VII); COVERAGE_PER_FILE_MIN_PERCENT=90 moves into ci/versions.env and is passed to orbital_drift.covcheck as --floor with an argv assertion, because ci/checks.sh:983 passes no floor today so the module default IS the gate bar and no test binds it (lowering it to 11.0 leaves the whole suite green); the errexit guard test extends to stage_coverage's window and the file's three-not-two errexit comments are corrected. (2) HYGIENE AND LOCKSTEP: PREFLIGHT_EXEMPT_PINS un-duplicated across its four hand-kept copies (ci/checks.sh:293, scripts/session_start_check.sh:52, that script's test parametrize, and the pin-a-tool skill), curing a live false `terraform: not installed (pinned 1.15.8)` pin-drift warning printed on every session start whose suggested remedy can never fix it; actions/cache bumped off deprecated Node 20; dependabot plus a weekly schedule so action pins and pip-audit stop drifting silently; .gitignore matches a symlinked .venv; decision-log chronological order restored and then enforced by a new governance test; single-home fixes for LABEL_COLUMNS, the five-times-copied ci/versions.env parser, and REPO_ROOT/_relative; FORBIDDEN_FLAGS renamed to the semantics its consumer already uses; provably redundant and unfalsifiable tests removed or strengthened. (3) BRANCH COVERAGE: branch coverage enabled through the repo's pins-plus-CLI pattern (never a [tool.coverage] section, which a guard test forbids) plus tests closing the highest-value uncovered branches, guard.py first. EXPLICIT LIMITS: unlocks no feature task — T013+ stays locked on G-1/G-2/G-3; no gate bar VALUE changes anywhere (the per-file 90 stands per RB-006; only its home and binding move); process track, outside the M0 feature budget on the RB-007(b) baseline; the AST guard-toolkit duplication and the entry-point parameterization refactor are deliberately NOT in this batch — both need their own decision doc | Ian (operator, in-session request 2026-08-22; recorded by agent)
2026-08-22 | RB-008a | EXECUTION RECORD under RB-008, not a new decision. spec-guardian review of the hygiene/lockstep PR (RB-008 part 2) returned a scope-record gap: four delivered items are not named in RB-008's enumeration, and one enumerated item is not delivered. (a) THE FOUR UNENUMERATED ITEMS STAND, as falling within the operator's standing in-session hygiene request of 2026-08-22 that RB-008 itself records: deriving traceability.py's row arity from the Row dataclass (CELLS_PER_ROW) is the same single-home class as the LABEL_COLUMNS and versions.env-parser fixes RB-008 does name, differing only in which literal was duplicated; scripts/guard_probe.sh's `set +e`/`set -e` window is a measured defect (that script's prologue is `set -u` ALONE, so the trailing `set -e` ENABLED errexit rather than restoring it) reached directly by RB-008's "provably redundant and unfalsifiable tests removed or strengthened" sweep; the new test_ci_contract.py errexit sweep is the durable half of that same fix; and tests/unit/shell_harness.py's PINS became a types.MappingProxyType with a test pinning that, which is a behaviour change (a stray write is now a TypeError) made because consolidating the versions.env parser turned PINS into one object imported by four modules whose docstring already claimed it was read-only — the enumerated de-duplication is what created the need. (b) REPO_ROOT/_relative single-homing, which RB-008 does enumerate, is DEFERRED, not silently dropped, and the earlier draft of this entry stated its premise wrongly: MEASURED at this commit, `REPO_ROOT: Final = Path(__file__).resolve().parents[2]` stands at THREE copies (covcheck.py:29, traceability.py:32, projections.py:40) and `_relative` at TWO (traceability.py:60, projections.py:161) — so REPO_ROOT has REACHED the repo's rule-of-three threshold rather than sitting inside it, and the deferral rests on cost and blast radius, not on the threshold. Single-homing them is not a de-duplication in place: covcheck, traceability and projections are siblings with no existing leaf all three can import (projections already imports planning.roadmap_data), so it requires creating a new shared module under src/orbital_drift and rewiring three production modules — a structural change of the same class RB-008 sends to its own decision doc. OWNER: its own follow-up PR under RB-008, to be logged before execution per rule 4; it is not carried by the gate-integrity or branch-coverage PRs. (c) Also recorded per rule 6: the weekly `schedule:` trigger RB-008 names moves OUT of this PR into the GATE INTEGRITY PR, which owns the `cancel-in-progress` fix — on refs/heads/main a scheduled run and a main push share one concurrency group, so shipping the trigger first would make it a recurring canceller of exactly the unscanned-main-push run RB-008 cites. .github/dependabot.yml carries no such coupling and lands here. (d) SECOND DEFERRAL WITH A NAMED OWNER, so it cannot become permanent by silence: the errexit sweep added here scans scripts/*.sh only, deliberately excluding ci/*.sh because ci/checks.sh's three errexit windows are the GATE INTEGRITY PR's subject and it is editing the same test file. That PR owns widening _SHELL_SCRIPT_GLOBS to include ci/*.sh, alongside the three-not-two errexit comment correction RB-008 already assigns it. Until it lands, ci/checks.sh's windows are covered by review only — they pass the sweep's rule today (the file opens `set -eu`), so nothing is known-broken, but nothing is mechanically watching either. (e) THIRD DEFERRAL WITH A NAMED OWNER, recorded in THIS file rather than only in a docs/decisions/ note because such a note is reached by no gate and by no reading protocol: MEASURED 2026-08-22 at 09a16b5 in a scratch copy, deleting docs/decisions/010-decision-log-text-drift.md outright leaves the full suite at 666 passed / 11 failed with a byte-identical failure list, its only inbound reference being a test docstring that cites the path in prose. THE GAP: drift in the TEXT of an entry in this log is unmechanized. The governance suite presence-checks IDs, mirrors (id, MM-DD) pairs into the governance skill in this file's order, and checks chronology; not one of them reads an entry's words, so an edit to RB-007(b)'s budget arithmetic, to RB-008's "no gate bar VALUE changes" limit, or to any EXPLICIT LIMIT clause passes every gate today. AND THE THREE MIRROR CHECKS ARE WINDOWED BY THE ARTIFACT THEY AUDIT: each filters on date >= the governance skill's own "Decisions since" heading, so the skill sets the scope of its own audit — MEASURED at 09a16b5, a ghost entry dated 2026-08-19 and mirrored NOWHERE in the skill leaves tests/governance at 149 passed and the full suite byte-identical to baseline, while the same line re-dated 2026-08-21 reddens exactly two tests; narrowing that window instead fails closed only through the order test's vacuity floor, whose message reads as a broken test rather than as a de-mechanization. PRECONDITION, cheaper than any mechanism and NOT executed here: the entry-text immutability rule everyone cites is stated in no RULE of this file — measured at 09a16b5 this file's RULES block runs 1-7 with nothing on entry text, and the only written statements of the rule anywhere are the tail of one assertion MESSAGE in test_decision_log_entries_are_in_chronological_order and a case-scoped parenthetical in docs/decisions/009-program-review-record.md:33 ("never rewrite the logged one", attached to the RB-007a taxonomy question and owned by the operator at PR #5 merge), neither of which is a rule a reviewer can cite generally; an earlier draft of this clause said NO governed document stated it and that the assertion message was its ONLY home, and 009:33 refutes both. Writing it as rule 8 is the prerequisite to mechanizing it; this clause REPORTS that prerequisite and does not state the rule, since amending the RULES block is a governance change of its own. OWNER: its own follow-up PR under RB-008, to be logged before execution per rule 4, carrying rule 8 and the immutability manifest together in one authorization because they are one decision about who may edit what; it is carried by neither the gate-integrity nor the branch-coverage PR. The rationale, the two rejected mechanizations and the manifest's cost to legitimate corrections live in docs/decisions/010-decision-log-text-drift.md. EXPLICIT LIMIT: records execution of already-approved scope plus three deferrals; unlocks nothing beyond RB-008; changes no gate threshold or bar VALUE; adds no rule to the RULES block and authorizes no manifest | Ian (operator, standing 2026-08-22 hygiene request; recorded by agent)
2026-08-22 | RB-008b | EXECUTION RECORD under RB-008, not a new decision. The weekly `schedule:` trigger on .github/workflows/ci.yml is enumerated under RB-008 part (2) HYGIENE AND LOCKSTEP ("dependabot plus a weekly schedule so action pins and pip-audit stop drifting silently"); it is executed in the part (1) GATE INTEGRITY PR instead, and this line records that move so the batch boundary stays auditable. PAIRED WITH RB-008a(c) — ONE MOVE, TWO SIDES, READ THEM TOGETHER: the DEFERRAL is already recorded from the sending side by RB-008a clause (c) on the hygiene/lockstep PR, which states that the weekly trigger moves OUT of that PR because a scheduled run and a main push share one concurrency group on refs/heads/main, and that .github/dependabot.yml carries no such coupling and stays there. This entry records the RECEIVING side: the trigger's arrival in the gate-integrity PR, landing in the same commit as the concurrency fix that is what makes it safe. The two agree on the fact and the reason and neither supersedes the other; hygiene merges first, so a reader meeting RB-008a(c) on main should follow it here for the coupling detail, and a reader meeting this entry should read RB-008a(c) for why the item left the batch it was enumerated in. Neither is a second authorization — RB-008 is the only decision, and both are execution records under it. WHY IT MOVED: the trigger is only safe alongside part (1)'s own concurrency fix. A scheduled run fires on refs/heads/main, so under the pre-existing ref-keyed group it shares a concurrency group with every push to main — with `cancel-in-progress: true` it cancels main's in-progress run outright, and even with cancellation disabled a run queued into an occupied group goes PENDING and is cancelled by the next arrival (GitHub's documented behaviour). Either path reproduces the RB-008 incident (run 32575049407, commit 4a76930, no completed gitleaks full-history scan, conclusion `cancelled` which never reads as `failure`), on a weekly timer. Landing the trigger in the hygiene PR would therefore have meant shipping it either before or after its own safety property, decided by merge order; the two land in one commit, with tests asserting per-commit grouping for non-PR events AND pull_request-only cancellation. The rest of RB-008 part (2) — dependabot, the actions/cache Node-20 bump, PREFLIGHT_EXEMPT_PINS de-duplication and the remaining single-home fixes — stays in the hygiene PR under RB-008a. ALSO RECORDED, per the RB-007a precedent of logging review-driven scope resolutions: two src/orbital_drift/covcheck.py edits in this PR originate from adversarial review rather than from RB-008's own enumeration, and the first of them changes a src/ module's USER-VISIBLE OUTPUT — which the "adds no scope" clause below would otherwise be read as denying. (i) Both per-file-floor messages format the bar as `{floor:g}` instead of `{floor:.0f}` (covcheck.py:82 and :112): RB-008 F4 makes the floor a CLI argument read from a text pin file, so a fractional bar is reachable, and `.0f` rounded `--floor 99.9` into "at or above 100%" — a message stating a bar the run did not enforce. Mechanically pinned by tests/unit/test_covcheck.py::test_a_fractional_floor_is_reported_exactly_not_rounded, so this is a checked claim and not prose (DoD rule 6). (ii) PER_FILE_FLOOR's doc comment claimed the per-file bar sits BELOW the global floor while 90 > 85; corrected to ABOVE and annotated FALLBACK-ONLY, since the gate now passes the pin explicitly. Both edits fall inside RB-008 part (1)'s already-named subject — orbital_drift.covcheck and the per-file-floor binding — and neither moves a gate bar VALUE: the per-file 90 stands per RB-006. EXPLICIT LIMIT: moves one enumerated item between PRs of the same authorized batch and records two review-driven edits to a module RB-008 part (1) already names; adds no scope, changes no gate bar VALUE, unlocks no feature task, and is not an authorization for anything RB-008 does not already enumerate | Ian (operator, RB-008 program approval; recorded by agent)
2026-08-22 | RB-009 | SECURITY FIX authorized out of cycle: the PreToolUse guard fails OPEN at its own segment ceiling. Measured 2026-08-22 at the enforcement boundary, not merely in-process — a payload of 256 nested command substitutions wrapping a denied command makes orbital_drift.guard.split_segments return ZERO segments (each nesting level costs two queue iterations, so 256 levels exhaust _MAX_SEGMENTS=512 before the fully-unwrapped branch ever appends a segment), whereupon analyze() iterates an empty list and returns not-blocked, and scripts/pretooluse_guard.sh exits 0 = ALLOW for a command the deny-list forbids in every mode; the identical payload at depth 1 exits 2 = BLOCK. The settings deny-list cannot catch it either, because its entries are prefix-shaped and the command begins with a substitution — precisely the "compound shape the tool-permission matcher cannot see" that guard.py's docstring says the module exists to catch, so its stated contract ("treats every failure to understand as a BLOCK"; analyze's own "Every uncertain path returns a BLOCK") is violated. Same defect class as the four measured fail-open bypasses RB-003 authorized the shlex rewrite to close. Fix: split_segments reports ceiling truncation to analyze(), which returns a C-1 BLOCK, mirroring the _MAX_DEPTH fail-closed precedent already at guard.py:245-246; regression tests pin the verdict in-process AND the wrapper's exit code at the boundary. EXPLICIT LIMITS: closes a fail-open, loosens nothing, changes no gate bar value and no allow-list, unlocks no feature task. BUDGET: security/process track, outside the M0 feature budget on the RB-007(b) baseline (DEC-002) — that counter stands at 4/4, and this PR does not consume it; stated as its own clause because "unlocks no feature task" is a different declaration from budget standing (RB-008 precedent, charter R-2). Owner authorization predates the work (the standing hygiene/triage request named below); this entry is logged after, landing with the commits it authorizes, per rule 6 | Ian (operator, standing hygiene/triage request 2026-08-22 under which the defect was found; recorded by agent)
2026-09-01 | RB-010 | Governance reconciliation + remediation program for PR#16 (T013+ application code — ingest/data/drift/train/registry/serve — merged 2026-08-23) and PR#17 ("Phase 0-R": hexagonal domain/ports/eval/observability/quality layer + dependency-contract fix, merged 2026-08-24). Both landed with no spec-guardian/adversarial-reviewer review, no G-1/G-2/G-3 (Gate Table requires G-1 before any T013+ task is even authored; README.md's own pre-merge status line stated "T006 authoring is deferred until [a] G-1 entry exists," and work proceeded past that stop sign through T042), no RB batch authorization, and DEC-002's M0 4-PR/16h budget counter already at 4/4 per the RB-007(b) baseline when PR#16 started — the condition R-2 (budget exhausted while a blocking G-x gate remains unresolved) exists to catch. NOT an R-5 recurrence: R-5 governs fix-cycle repetition during an active review cycle, and no review cycle ever started on either PR. A six-lens SDLC peer review (governance/task-mapping, code-correctness/security, config-wiring, architecture, DevOps/security-ops, QA/data-lifecycle; six independent deep-dive agents, cross-checked against each other, two direct inter-agent contradictions personally resolved against source) found, among other defects: a NON-NEGOTIABLE Principle II violation (eval/bootstrap.py, eval/superiority.py hand-roll statistical resampling/promotion-gate logic with no scipy/arch/statsmodels import; CHANGELOG.md falsely claims full Constitution-II compliance for all of eval/); a Dockerfile that cannot build (src/ is never copied into the builder stage before pip wheel runs) and, if that were fixed, cannot run either (no extras installed, so fastapi/uvicorn/torch are absent despite ENTRYPOINT invoking uvicorn) — invisible to all 14 passing CI gates; a drift/trigger.py stuck-breaker (is_retraining_active has no failure-path reset, so one failed retrain permanently disables all future drift-triggered retraining with zero visibility, directly threatening Constitution VI's unattended-soak requirement); a serve/app.py with no auth/rate-limiting/request-size bounds and no startup wiring (cannot serve a single real inference request as shipped); config.py effectively unwired repo-wide with measured drift already occurring between it and hardcoded module defaults (gradient_accumulation_steps 2-vs-1, serving_port 8080-vs-8000 with a non-matching env var name); two fully-built but never-CI-wired "phantom gates" (quality/hardcode_scan.py, 102 real findings; .importlinter's contracts via tests/architecture/, never given a CI stage); a citation-integrity defect (pyproject.toml cites "RB-009" as authorization for its dependency-contract fix, but the real RB-009 above is the unrelated PreToolUse-guard fix; pyproject.toml also references a dep_contract module, a deps CI stage, and tests/unit/test_dependency_contract.py, none of which exist — confirmed directly); registry/ops.py and drift/trigger.py concurrency races with no locking anywhere (same bug class already found and fixed once in this repo via eval/spatial.py's _MORAN_LOCK, but the same scrutiny was never applied here); observability/logging.py's credential redaction crashing-and-silently-dropping the log line on any NamedTuple value (every result type this codebase defines) and fully bypassing any dataclass/pydantic object including OrbitalDriftConfig itself, with structured/redacted logging wired into eval/ only — every other pipeline module uses unredacted stdlib logging; all 5 hexagonal ports (ports/{registry,dataversion,catalog,tiles,compute}.py) structurally disconnected from their "real" module counterparts, 0-for-5 having an actual adapter, including two separate unconnected implementations of "the registry"; and the repo's only CT-loop lifecycle test (test_user_journey_ct_loop.py) never calling the superiority gate before promoting a candidate. Full findings, severity ratings, and file:line citations recorded in this session's approved plan. DISPOSITION of the already-merged work: RECONCILE FORWARD, mirroring the RB-006 precedent — not revert. tasks.md's existing AUTHORED-PROVISIONAL status (previously used only for T006) is applied retroactively to T013-T052 pending retroactive spec-guardian + adversarial-reviewer review before any checkbox flips to [x]. AUTHORIZES a 14-part remediation program, each part owned by an agent per the CLAUDE.md roster and requiring its own spec-guardian + adversarial-reviewer pass and its own green `sh ci/checks.sh` run before completion: (1) governance/docs reconciliation (tasks.md, traceability.md, README.md, ARCHITECTURE.md, CHANGELOG.md, pyproject.toml's false citations, plan.md's module map, CLAUDE.md's roster gap); (2) Principle II options memo (docs/decisions/011-*.md) — code limited to a failing stub; the operator reads the memo and logs the method+interpretation decision as its own entry before (3) Principle II implementation may start; (4) config.py hardening (credential defaults, numeric bounds); (5) per-module config wiring, depends on (4); (6) hardcode_scan CI-wiring, depends on (4)+(5); (7) dep_contract module + deps stage completion; (8) import-linter CI-wiring; (9) logging-redaction fix + structured-logging rollout beyond eval/; (10) registry/ops.py runtime validation + locking; (11) drift/trigger.py stuck-breaker fix + locking; (12) Dockerfile build fix + a docker-build CI smoke stage; (13) serve/app.py baseline hardening, depends on (5); (14) promotion-gate test coverage, depends on (3). EXPLICIT LIMIT: does not create G-1/G-2/G-3 — only the operator's execution of T003/T005/T012 can; unlocks no new Phase 1+ authoring beyond what already exists in the ungated T013-T052 work being reconciled; process/remediation track, outside the M0 feature budget on the RB-007(b) baseline; the Principle II method choice is tracked separately via Part 2's memo and is explicitly NOT authorized by this entry; does not itself resolve AR-3 (dataset choice), the container-registry strategy, or the lakeFS/MLflow-adapter-disposition questions — those remain open operator decisions named in the approved plan's forward-roadmap section, to be logged separately when decided. Operator approval given in-session (a plan covering this exact program was reviewed and approved via ExitPlanMode) before work started; logged after per decision-log rule 6 | Ian (operator, in-session plan approval; recorded by agent)
2026-09-03 | RB-011 | Full-suite QA triage authorized on branch `qa/full-suite-triage` (cut from `main` @ 84d45ed, fast-forwarded from 63413f8 immediately before branching). Scope: baseline every CI gate (specs/projections/lint/typecheck/dead/audit/traceability/governance/unit/contract/smoke/gitleaks/hooks/coverage) on `main`, plus the three GPU-gated live tiers (sanity/integration/e2e — confirmed executed for real on the dual-RTX-5060-Ti/5060 host, not capability-skipped, all passing) and an ad-hoc uncommitted live-STAC probe against the real Earth Search endpoint (never committed under `tests/`, per the zero-skip guard's closed capability-guard allowlist). Findings recorded in `docs/incidents/2026-09-03-full-suite-triage.md`. EXPLICIT LIMIT: this entry authorizes the triage/RCA report only — it fixes nothing and unlocks no src/ change; each RCA finding a reviewer approves lands as its own separate task/PR with a failing regression test first, per CLAUDE.md's one-task-per-branch and TDD rules | Ian (operator, autonomous execution per approved plan; recorded by agent)
2026-09-03 | RB-011a | EXECUTION RECORD under RB-011, not a new decision. Same-day follow-up on both RB-011 findings. Finding 1 (mypy `torch.amp` attr-defined) resolved by investigation alone, no code change: fetched the pinned `torch==2.13.0` release's actual source (github.com/pytorch/pytorch v2.13.0 tag) and confirmed `torch/amp/__init__.py` declares `__all__ = ["autocast", ..., "GradScaler"]`; the local host's stale pre-release `torch` build lacks that declaration, so the error is a confirmed environment artifact that will not reproduce in CI or on a correctly-pinned install. Finding 2 (Windows path-separator bug in `test_coverage_positive_control.py`) fixed on branch `fix/coverage-positive-control-windows-path-sep` (commit 668dc25): normalized `_summary()`'s report-key lookup via `Path(...).as_posix()`, added `test_summary_lookup_is_path_separator_agnostic` (a synthetic, OS-independent regression test so a future regression is caught on Linux CI too), verified TDD red→then→green, and re-ran the full `unit` stage green (946 passed, 0 failed, same 2 legitimate capability-guard skips). Not yet merged at that step; see RB-011b (same day) for the POSIX correction in 149cc85 and the final unit re-run at 947 passed / 0 failed. EXPLICIT LIMIT: records the disposition of RB-011's two findings; authorizes no additional scope beyond what RB-011 already named | Ian (operator, autonomous execution per approved plan; recorded by agent)
2026-09-03 | RB-011b | EXECUTION RECORD under RB-011, not a new decision. PR #19's Linux CI caught a defect the local Windows host could not see: RB-011a's v1 fix normalized report keys with `Path(...).as_posix()`, which is a NO-OP for backslashes under POSIX semantics (backslash is a legal filename character there, not a separator), so the new backslash-keyed regression test passed on Windows but failed in Linux CI (run 33784050330: 947 passed / 1 failed — the inverse of the original Windows-only bug). Corrected in commit 149cc85 on the same branch: the key normalization is now a string-level `key.replace("\\", "/")`, identical on every OS (verified under POSIX `PurePosixPath` semantics), and the regression test is parametrized over BOTH separators so both key shapes are pinned on every host. Re-verified locally: test file 11/11 pass (both variants), lint green, full `unit` stage 947 passed / 0 failed. PR #20's all-green CI (including `typecheck`) independently confirms Finding 1's environmental resolution. EXPLICIT LIMIT: records a review-iteration fix inside RB-011's already-named scope per the RB-007a precedent; authorizes no additional scope | Ian (operator, autonomous execution per approved plan; recorded by agent)
2026-09-03 | RB-012 | Full-suite QA round-2 triage authorized on branch `qa/full-suite-triage-r2` (cut from `main` @ 4b6ac35, the PR #20 merge that closed RB-011; the round-1 branch's remote was merged and deleted). Operator request given in-session 2026-09-03 BEFORE work started ("compare to origin — create new branch — run all tests and e2e with and without mocks and live LLM API & GPU-based changes to origin — triage RCA and fix all defects caught and add regression and QA"); logged after per rule 6, landing with the commits it authorizes. Scope: re-baseline every CI gate stage on the post-RB-011 main (stages run individually for full visibility, since `all` stops at first red; `all` reserved for the final green proof), plus the GPU-gated live tiers (sanity/integration/e2e — to be executed for real on the dual-RTX-5060-Ti/5060 host against live docker-compose lakeFS/MLflow/MinIO services, not capability-skipped) and an ad-hoc uncommitted live-STAC probe against the real Earth Search endpoint (never committed under `tests/`, per the zero-skip guard's closed capability-guard allowlist). LLM scope: verified by whole-repo search that this codebase has NO LLM API surface (no openai/anthropic/litellm dependencies; no network calls in the guard scripts) — the request's "live LLM API" is therefore discharged as live external surfaces generally (Earth Search STAC, lakeFS, MLflow, dual-GPU execution) and the absence is recorded as an explicit report note, not a defect. Known-benign baseline carried from RB-011: 2 capability-guard MSYS skips; smoke declared-empty; unit runs twice under `all`. RB-010 deferred items (eval/bootstrap.py + eval/superiority.py Principle II hand-rolled statistics; the Dockerfile build defect) are out of scope — if surfaced they are classified expected-known with an RB-010 citation, not fixed under this entry. Findings recorded in `docs/incidents/2026-09-03-full-suite-triage-r2.md`. EXPLICIT LIMIT: authorizes the triage/RCA report only — it fixes nothing and unlocks no src/ change; each RCA finding lands as its own separate task/PR with a failing regression test first, per CLAUDE.md's one-task-per-branch and TDD rules | Ian (operator, in-session request; recorded by agent)
2026-09-03 | RB-012a | EXECUTION RECORD under RB-012, not a new decision. Round-2 full-suite triage executed and closed same-day with ZERO new defects. Stages run individually (17 distinct stages, canonical `stage_all` order) because `all` stops at first red and would have hidden everything behind the one known artifact: 16 of 17 green (lint, unit 949 passed/0 failed, contract, smoke, coverage 1159 passed/0 failed at 98.89% global with every measured file ≥90% per-file, gitleaks, dead, audit, specs, traceability, projections, governance 163 passed, hardcode, deps, architecture, hooks). The single red stage, `typecheck`, is round-1's Finding 1 exactly (mypy `torch.amp` attr-defined ×2, `baseline.py:279,285`): re-verified at mechanism level on this host — the installed `torch==2.12.0.dev20260408+cu128` nightly's `torch/amp/__init__.py` has no `__all__` (probe output: `False`), while RB-011a already confirmed the pinned `torch==2.13.0` declares it; PR #20's CI `typecheck` job is green on this same source. Environmental, no code change, same disposition as RB-011a. Live tiers executed for real, not capability-skipped: sanity 3/3, integration 3/3 (incl. dual-GPU train/serve isolation), e2e 2/2 (full CT + canary-serve and CT + rollback lifecycles) — 8/8 passed; the only skips anywhere are the two closed-allowlist capability-guard MSYS entries. Ad-hoc live-STAC probe against the real Earth Search endpoint: 4 real sentinel-2-l2a scenes in 0.52 s. Two scope notes recorded in the report body: (1) the request's "live LLM API" is verified absent from this codebase (no LLM dependency, no network call in the guard scripts) — noted, not a defect; (2) live lakeFS/MLflow services were deliberately not brought up — docker-compose.yaml defines only the serving app (whose build is the deferred RB-010 Part 12 defect), and source inspection confirms `LakeFSOps.commit_scene` never calls its configured endpoint (local SHA-256) per RB-010's "simulated, not real" disposition. No fix branches were needed, so no regression tests beyond round 1's merged RB-011b artifact (which passed in both unit and coverage). Report: `docs/incidents/2026-09-03-full-suite-triage-r2.md`. EXPLICIT LIMIT: records execution of RB-012's already-named scope; authorizes no additional scope, fixes nothing, unlocks no src/ change | Ian (operator, in-session request; recorded by agent)
